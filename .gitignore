# 대용량 원본/생성 데이터 (빌드 시 생성, 저장소에 넣지 않음)
data/
tiles/
*.mbtiles
*.osm.pbf
*.tif

# 지오코딩 인덱스 (07-gen-geocode.py 생성물)
geocode/*.sqlite

# Planetiler / vendor 바이너리
planetiler/*.jar
vendor/

# 글리프 폰트 PBF (다운로드 산출물, 수만 개 소파일)
style/glyphs/

# Docker 산출 tar 번들
*.tar
dist/

# OS / 에디터
.DS_Store
*.log
node_modules/
__pycache__/

# build-style.sh 가 생성 (조각 병합 결과 — 브랜치 병합 충돌 방지 위해 비커밋)
style/style.json

# 로컬 에이전트/IDE 설정
.claude/
.idea/
*.bak
