# 역지오 도로명축 합성PNU 키조인 — 실측 근거

`/reverse` 의 도로명축을 KNN 최근접에서 **PIP 필지 내 합성PNU 키조인 우선 / KNN 폴백** 으로
바꾼 근거 수치다. (T029)

측정: 지적도 `ST_PointOnSurface` 로 독립 생성한 **2,401 좌표** — 3지역(종로/기장/완주) × 4지목
(대·도·임·전답) × 200 + 결정타 1. 변경 전후를 같은 스크립트로 호출해 대조했고, 변경 후는
**2회 독립 측정**해 아래 수치가 두 회차 완전 일치함을 확인했다.

> 595건 벤치마크(`scripts/595-corrections.csv`)는 질의 좌표 자체가 우리 순방향 출력이라
> 자기참조다. 역지오 정확도 근거로 쓰지 않는다.

## 1. 왜 raw 가 아니라 합성 PNU 인가

`address.bd_mgt_sn` 의 앞 10자리에는 **폐지된 법정동코드**가 박혀 있다. 그래서
`substr(bd_mgt_sn,1,19)` 로 그대로 조인하면 코드가 바뀐 지역에서 무너진다. 현재 코드가 든
`bcode` 를 앞에 붙여 만든 합성 PNU `bcode || substr(bd_mgt_sn,11,9)` 는 그 영향을 받지 않는다.

표본 2,401 PNU 를 `address`(`kind='addr'`)에 직접 대조한 결과:

| 지역 | raw | 합성 | 배율 | **대(垈)만** raw → 합성 |
|---|---|---|---|---|
| 종로 | 164/800 (20.5%) | 163/800 (20.4%) | ×0.99 | 123 → 122 |
| 기장 | 77/800 (9.6%) | 123/800 (15.4%) | ×1.60 | 61 → 98 |
| 완주 | **3/800 (0.4%)** | **121/800 (15.1%)** | **×40.3** | **2 → 101 (×50.5)** |
| 합계 | 244 | **407** | ×1.67 | |

완주 대지가 **1.0% → 50.5%** 로 종로 61.0% 에 근접한다. 법정동코드가 바뀌지 않은 종로에서는
두 방식이 거의 같고(합성이 1건 적다 — `bcode` 와 `bd_mgt_sn` 앞 10자리가 어긋난 개별 행으로
보이나 행 단위 규명은 하지 않았다), 코드가 바뀐 지역에서만 차이가 폭발한다. 원인 가설과
수치가 정확히 일치한다.

DB 기준 조인가능 **407 == 실제 응답 `address_source="pip_key"` 407**. 조인 가능한 곳에서
빠짐없이 조인됐다.

## 2. 왜 KNN 폴백이 부가기능이 아니라 정확성 요건인가

주소점(`kind='addr'`)은 건물에 부여되므로 도로·임야·전답 필지에는 대개 없다. 전국 필지의
**16.28%** 가 도(道)다. 폴백 없는 순수 키조인 전환은 도로 위 좌표에서 응답이 사라지는 회귀가
된다.

도(道) 표본 실측:

| 지역 | n | HTTP 200 | `knn` 경로 | `road` 빈 문자열 |
|---|---|---|---|---|
| 종로 도 | 200 | 200 | 195 | **0** |
| 기장 도 | 200 | 200 | 193 | **0** |
| 완주 도 | 200 | 200 | **200** | **0** |

침묵(빈 도로명) 0건. 그래서 **0행은 오류가 아니라 정상 경로**로 다루며, 거리 임계도 두지 않는다.

도 지목에서도 소수(종로 5·기장 7)가 `pip_key` 인데, 도로 필지 안에 주소점이 실제로 있는
경우다(예: 종로 수송동 146-8 → 구두수선대).

## 3. `WITH cand AS MATERIALIZED` 는 문법 장식이 아니다

빼면 PG12+ 가 CTE 를 인라인하고, 플래너가 `address_addr_geom_gix`(geom) 를 골라 합성키를
`Index Cond` 가 아닌 **`Filter` 로 강등**시킨다. 그 상태에서도 EXPLAIN 에는 인덱스명이 버젓이
나오므로 "인덱스명이 보인다" 만 확인하는 검증은 이 결함을 통과시킨다. 확인해야 할 것은 4가지다.

| # | 기준 | 조인 성공 | 0행 |
|---|---|---|---|
| 1 | `address_synth_pnu_idx` 등장 | ✅ | ✅ |
| 2 | 합성키가 **`Index Cond`** | ✅ | ✅ |
| 3 | `address_addr_geom_gix` **미등장** | ✅ | ✅ |
| 4 | 0행도 1~3 만족 + 같은 자릿수 | — | ✅ 0.152ms |

```
Index Scan using address_synth_pnu_idx on address a
  Index Cond: ((bcode || substr(bd_mgt_sn, 11, 9)) = '1111014800100010036'::text)
```

질의 시간 cold 85.9ms / **warm 0.143ms**, 0행 0.152ms(0행이 더 빠르다 = 전주행 없음).
생성 비용은 **34초 / 300 MB**(heap 5,722 MB 의 5.2%) — `kind='addr'` 부분 인덱스라
전체 1,628만 행이 아닌 1,069만 행만 담는다.
인덱스 실사용은 `pg_stat_user_indexes.idx_scan` **17 → 4,813**(2,401×2회)로 확인했다.

## 4. `ANALYZE` 로는 선택도 추정이 고쳐지지 않는다 — `CREATE STATISTICS` 가 필요하다

부분 인덱스를 만들고 `ANALYZE address` 를 돌려도 행 추정은 `rows=53029` 로 고정이었고
**플래닝에만 75.1ms** 가 들었다.

PostgreSQL `selfuncs.c` 의 `examine_variable()` 은 **부분 인덱스의 표현식 통계를 선택도 추정에
사용하지 않는다.** 표현식 인덱스가 자체 통계를 갖는 것은 맞지만 `WHERE kind='addr'` 이 붙는
순간 그 통계는 추정 경로에서 배제되고 `DEFAULT_EQ_SEL`(0.005)이 그대로 쓰인다.

해법은 PG14+ 의 독립 확장통계다 (`schema/11-address-search.sql` 에 동봉):

```sql
CREATE STATISTICS IF NOT EXISTS address_synth_pnu_stat
  ON (bcode || substr(bd_mgt_sn, 11, 9)) FROM address;
```

플래닝 **75.1ms → 0.119ms**, 행 추정 **53029 → 1**(실측 평균 1.16행). 인덱스를 되돌릴 때는
`DROP STATISTICS` 도 함께 필요하다.

## 5. 응답이 실제로 어떻게 달라졌나

| 수치 | 값 |
|---|---|
| `parcel` 동일 + `road` 만 변경 | **48** |
| before 빈 `road` → after 채워짐 | 0 |
| before 값 → after 빈 문자열 (**회귀**) | **0** |
| `parcel` 이 바뀐 좌표 (지번축 회귀) | **0** |

**회귀 0건**이 이 표의 요점이다. 순증 0인 것은 표본이 필지 대표점이라 2,401건 전부 KNN 이
2.5km 안에서 주소점을 찾았기 때문이다(before 에 빈 `road` 가 애초에 없었다).

바뀐 48건이 개선인지는 축을 명시해야 말할 수 있다. 시군구 축으로는 before/after 가 전부 같아
**가릴 수 없다**. 그래서 응답 자신이 말하는 사실을 축으로 삼았다 — before 의 도로명은 정의상
KNN 최근접 주소점에서 오고, 그 최근접점의 필지는 `nearest[0].address.parcel` 에 찍혀 있다.

| 구분 | 건수 |
|---|---|
| before KNN 최근접점이 **남의 필지**였음 | **34** |
| before KNN 도 같은 필지 (같은 필지 안 다른 주소점) → **보류** | 1 |
| `nearest[0].address.parcel` 이 `null` 이라 판정 불가 → **보류** | 13 |

즉 **34건 증명 / 14건 보류**다. 그중 6건은 도로 이름 자체가 바뀌었다:

```
P 서울특별시 종로구 홍지동 69-39
B 서울특별시 종로구 홍지문길 51 (보현빌라)              ← 남의 필지
A 서울특별시 종로구 홍지문2길 20 (상명대학교 종합강의동)

P 서울특별시 종로구 신문로2가 170
B 서울특별시 종로구 송월길 18-7                         ← 남의 필지
A 서울특별시 종로구 새문안로 43-12 (I6)
```

또한 `pip_key` 407건 중 **62건**은 KNN 최근접점이 남의 필지였다(조인이 없었다면 섞일 수 있던
구간). 그중 34건은 도로명까지 달랐고 28건은 값이 같았다 — 집합건물 등 같은 도로명주소를 여러
필지가 공유하는 경우다.

> **PIP 가 항상 옳다는 주장은 하지 않는다.** 독립 교차검증에서 PIP 자체의 주소 복원율은
> 76.7% 다. 위 34건은 "after 가 더 좋은 주소" 라는 주장이 아니라 **"before 는 다른 필지의
> 도로명이었다"** 는 검증 가능한 사실이다. 두 주장은 다르며 후자만 근거가 있다.

## 6. 미해결 관측 — 부하 하에서의 소량 503

변경 후 측정에서 비200 응답이 **1회차 5건 / 2회차 4건**(2,401 중) 나왔다. 변경 전 측정에서는
**0건**이다.

- 실패 좌표가 회차마다 완전히 다르다 → 좌표 특성이 아니다
- 실패 좌표를 개별 재호출하면 **전건 200**(0.06~0.15초)
- 컨테이너 로그에 조인 실패 NOTE **0건** → 조인 질의가 예외를 낸 적은 없다
- postgres 로그의 원인 질의는 **KNN(`addr_at`) 쪽**이다:
  `could not attach to dynamic shared area` / `terminating background worker "parallel worker"
  due to administrator command` = `statement_timeout`(3s) 초과 취소

조인 질의가 늘린 DB 부하 때문에 기존 KNN 질의가 3초 벽에 닿는 빈도가 생겼다는 해석이 관측과
정합하지만, 부하 수준별 대조를 하지 않았으므로 **가설이지 증명이 아니다**. 8워커 동시 호출이라는
극한 조건에서만 나타나며 단건·순차 호출에서는 재현되지 않는다. 운영 전 재확인이 필요하다.

## 7. 재현

컨테이너 배선은 `docs/geocode-verify-container.md` 를 따른다. 검증 스크립트 호출 시
`OURS=http://127.0.0.1:8093` 을 **반드시** 앞에 붙인다 — 미지정 시 기본값이 운영 서버다.

좌표 집합은 한 번 만든 뒤 고정하고(before/after 가 같은 좌표여야 대조가 성립한다), 변경 전
측정을 **코드 수정 전에** 끝낸다.

## 8. 개선 전후 「필지 소속 일치율」 — 완료조건 #4

§5.4 가 낸 `pip_key` 407/407 은 **이 지표가 아니다.** 그것은 "조인이 발화한 응답의 지번축이
표본 필지와 같은가"이고, 지번축은 조인과 무관하게 PIP 가 정하므로 정의상 100% 여야 한다 —
구현 무결성 점검이지 개선폭 측정이 아니다. 완료조건 #4 가 묻는 것은 **응답이 가리키는 주소점이
질의 좌표가 속한 필지 안에 있는가가 개선 전후로 얼마나 달라졌는가**이다. 아래가 그 측정이다.

### 8.1 측정 설계

| 항목 | 값 |
|---|---|
| 좌표원 | `ST_PointOnSurface(parcel.geom)` — 서울(`sido='11'`) 필지 무작위 1,000 |
| 추출 | `ORDER BY md5(pnu)` (결정적) · 지목 층화 **없음**(모집단 그대로) |
| before | baseline `1782bf4` 의 `server/geocode-api-pg.py` |
| after | 현행 워크트리 코드 |
| 소속판정 | `ST_Contains(parcel.geom, 응답 주소점 geom)`, 기준 필지는 **좌표를 낸 원본 필지** |

`scripts/595-corrections.csv` 는 쓰지 않았다 — 그 좌표는 우리 순방향 출력이라 자기참조다.

지목 구성은 대 736 · 도 164 · 임 22 · 전 19 · 잡 10 · 답 9 · 철 8 · 천 7 · 공 7 · 구 5 ·
제 3 · 종 3 · 주 2 · 차 2 · 학 2 · 장 1. 대(垈)는 층화가 아니라 **부분집합**이며, 아래에서
전체와 별도로 집계한다.

### 8.2 방법 — API 실측 + 코드 원문 SQL 재현 하이브리드, 그리고 그 한계

순수 API 실측만으로는 이 지표를 낼 수 없다. **`/reverse` 응답이 도로명축 주소점의 좌표를 내주지
않기 때문이다** — `ROAD_SIDE_STRUCT_KEYS` 9필드에 `b_code` 조차 없어 응답만으로는 어느
`address` 행이었는지 역식별도 불가능하다. 그래서 이렇게 나눴다.

1. **어느 경로가 발화했는지는 API 가 알려준 대로 따른다.** 추정하지 않고 응답 최상위
   `address_source`(`pip_key` / `knn`)를 그대로 쓴다. before/after 양쪽을 실제로 호출했다
   (baseline 은 별도 컨테이너 `t029-geocode-pg-base`, 현행은 `t029-geocode-pg`).
2. **소속판정에 쓸 geom 은 SQL 로 얻는다.** 이 SQL 은 근사가 아니라 `server/geocode-api-pg.py`
   의 `addr_at()`·`parcel_at()`·`road_at_parcel()` **원문을 그대로 옮긴 것**이다
   (bbox `ST_Expand(pt, 0.035)` + `ST_DWithin(geography, 2500)` + `WITH cand AS MATERIALIZED`
   포함). 검수자가 허용한 `ST_DWithin` 단독 근사는 **쓰지 않았다** — 근사와 실제의 차이를
   따로 논증할 필요를 없애기 위해서다.
3. **재현이 충실한지를 실증한다.** API 응답의 도로명축 서명(도로명·본번·부번·우편번호)과 재현
   행의 같은 네 값을 전건 대조했다. **before 1000/1000 · after 1000/1000 일치** — 재현 행이
   API 가 실제로 쓴 그 행이라는 뜻이다.

**한계 — 그대로 적는다.**

- **소속판정의 기준 필지는 PIP 결과가 아니라 좌표를 낸 원본 필지다.** 둘이 갈리면 지표가
  낮게 나올 수 있으나, 이 표본에서는 **PIP 가 원본 필지를 되찾은 비율이 1000/1000** 이라
  실제로는 같은 값이다.
- 서울 단일 시도 표본이다. §5 의 3지역(종로·기장·완주) 대조와 모집단이 다르므로 **수치를
  직접 비교하지 말 것.**
- `after_gap_m` 는 `ST_Distance(폴리곤, 점)` 이라 점이 폴리곤 안이면 0 이다. 이격 통계는
  **필지 밖 건에 대해서만** 의미가 있다.
- 부하 8워커 병렬 호출을 썼다. §6 의 소량 503 관측 조건과 겹치지만 이번 실행에서는 **API 오류
  before 0 / after 0** 으로 한 건도 나지 않았다.

### 8.3 결과

| 표본 | before | after | 차이 |
|---|---|---|---|
| **전체 (n=1000)** | 526 (52.6%) | **548 (54.8%)** | **+2.2 pp** (+22건) |
| 대(垈) 부분집합 (n=736) | 515 (70.0%) | **535 (72.7%)** | +2.7 pp (+20건) |
| 대 이외 (n=264) | 11 (4.2%) | 13 (4.9%) | +0.8 pp (+2건) |

전이 내역(전체): 유지OK 525 · **개선 23** · **퇴행 1** · 유지NG 451.

경로 분포는 `pip_key` 575 (57.5%) / `knn` 425 (42.5%). 지표 변화는 **전적으로 `pip_key`
575건 안에서 일어난다** — 496 → 518. 이를 두 방향으로 교차확인했다.

- 도로명축 응답이 before↔after 로 달라진 46건(4.6%)은 **전부 `pip_key`** 다
  (`road_changed=1` 이면서 `address_source≠pip_key` 인 건 **0**).
- `knn` 425건은 before/after 소속판정이 **한 건도 갈리지 않았다**(0건). 폴백 경로가 baseline
  과 동일하게 동작한다는 실증이다.

그리고 **바뀐 46건 안에서** before 소속 1/46 (2.2%) → after 23/46 (50.0%). 이 태스크가 실제로
건드린 표면에서는 소속률이 절반으로 올라간다.

### 8.4 개선폭이 작다는 사실

전체 +2.2 pp 는 작다. 숨기지 않고 적는다. 작게 나오는 구조적 이유는 두 가지다.

1. **표본의 42.5%가 조인 자체에 도달하지 않는다.** 도(道)·임야·하천 등에는 필지에 매인 주소점이
   없어 KNN 으로 폴백하며, 그 경로는 코드가 바뀌지 않았으므로 지표도 움직이지 않는다(위 0건 확인).
   대 이외 지목의 소속률이 before 4.2% 에 머무는 것이 그 그림이다.
2. **`pip_key` 575건 중 496건은 before 에도 이미 맞았다.** KNN 이 최근접을 고르는 이상 대부분의
   좌표에서는 같은 필지 주소점이 최근접이기도 하다. 조인이 값을 바꾸는 것은 그 둘이 갈리는
   가장자리뿐이고, 그 가장자리가 46건이다.

즉 이 태스크의 값어치는 소속률 절대값이 아니라 **§5 가 보인 혼합응답 제거**(지번축과 도로명축이
서로 다른 필지를 가리키던 48건 정정, 회귀 0)에 있다. 소속률은 그 부수효과로 소폭 오른다.

참고로 검수자가 3지역 대필지 600좌표로 낸 값은 before 295(49.2%) → after 302(50.3%), +1.2 pp
였다. 모집단이 다르므로 수치는 다르지만 **방향과 자릿수가 일치**한다.

### 8.5 키조인은 「같은 필지 안」을 보장하지 않는다 — 실측

계획 문서가 한때 「필지 안에 있다는 사실 자체가 거리보다 강한 근거다」라고 적었는데, **그 문장은
조인이 실제로 보장하는 것보다 강하다.** 조인이 보장하는 것은 **키(법정동코드+본번·부번) 일치**이지
**기하 포함**이 아니다. 두 축의 출처가 다르기 때문이다 — 필지 경계는 연속지적도, 주소점은 도로명
주소 DB 이고, 서로 다른 시점·기준으로 만들어진다.

이 표본의 실측:

| 지표 | 값 |
|---|---|
| 키조인 성공 | 575건 |
| 그중 **기하적으로 필지 안** | **518건 (90.1%)** |
| 필지 밖 | 57건 |
| 필지 밖 이격 | 최대 **56.6 m** · 중앙 1.9 m · 30 m 초과 3건 |

대표 사례 — 강북구 `1123010400105580083`(대). KNN 은 4.5 m 의 주소점을 골랐고 그것이 마침
필지 안이었다. 조인은 같은 필지 키를 가진 다른 주소점(7.3 m)을 골랐고 그것은 경계 **밖 1.4 m**
였다. 이 표본의 **유일한 퇴행 1건**이 정확히 이 형태다.

**그럼에도 거리 임계를 넣지 않는 설계는 유지한다.** 임계는 이 57건을 KNN 으로 되돌리는데, KNN 은
「같은 필지 키」라는 보증조차 없이 최근접만 보므로 §5 가 고친 혼합응답을 되살린다. 90.1% 대
57건, 그리고 그 57건 대부분이 수 m 이내(중앙 1.9 m)라는 것이 이 교환의 실제 크기다. 다만
**보증의 성격을 정확히 말해야 한다** — 키조인이 주는 것은 「행정적으로 같은 필지에 등록된
주소점」이지 「기하적으로 그 필지 안의 점」이 아니다.

### 8.6 재현

측정 스크립트는 `docs/geocode-verify-container.md` 의 (라) 절차로 baseline 컨테이너
(`t029-geocode-pg-base` / :8094)를 함께 띄운 상태를 전제한다. 실행판 대조
(`docker exec … md5sum /app/geocode-api-pg.py`)를 **측정 전에** 하고, 끝나면
`docker rm -f t029-geocode-pg-base` 로 baseline 컨테이너를 지운다.
