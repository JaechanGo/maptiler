# 지오코더 정확도 외부기준 검증 — VWorld 대비 (T046)

측정 실시 2026-08-21. 측정 대상은 **8092 PostGIS 판**이다.
측정 스크립트는 커밋 `41298f7` (`scripts/t046/*.py`, 테스트 278 개)에 있다.
**커밋 SHA 는 D 단계 리베이스로 바뀌었다** — 신구 대응은 §12.1 을 보라.

> ### ⚠ 전면 정정 (2026-08-28 · T050)
> **본 측정 12,000 건 전체가 사전 로더 5 종이 미기동된 런타임에서 수행됐다** (T047 전수
> 실측으로 확정, VWorld 재호출 0 건). 리 관련 결론(§5.5 · §6.4 · §6.7.5 · §7.7 해석부 ·
> §9 일부 · §10.1)은 **런타임 결함이 아니라 측정 인스턴스의 부팅 침묵 실패의 산물**이다.
> 오염/유효 분리는 **§0.0**, 재측정 설계는 §0.0.3 을 보라. 원문은 수정하지 않고
> `[정정 T050]` 블록으로 주해한다.

---

## 0. 먼저 읽어야 할 한계 — 이 보고서가 재지 *못한* 것

이 절은 각주가 아니다. 아래 네 가지를 모르고 뒷장의 숫자를 읽으면 반드시 오독한다.

### 0.0 ★ 전면 정정(2026-08-28 · T050) — 측정 런타임이 사전 미기동 상태였다

**사실 (T047 C0 실측, 전수·VWorld 재호출 0 건).** 측정 인스턴스(2026-08-21T04:45:26Z 기동)는
PostGIS 동시 기동 경합으로 `_boot_check` 의 `with POOL.connection()` 이 **30 초 타임아웃**
(`POOL` 기본값)으로 죽었고, 그 블록 안의 사전 로더 5 개가 전부 건너뛰어졌다. 로그 실증:
`2026-08-21T04:46:14Z WARNING table check failed: couldn't get a connection after 30.00 sec`.
관측된 5 회 기동 중 1 회(20%)가 이 상태였다. 동시에 꺼진 것:

- `_HAS_LAWD_RI=False` → **리 좁힘 비활성 + `ri_nm` 미조회** (§5.5 · §6.4 · §6.7.5 의 근원)
- `_HAS_SIDO_REMAP=False` → 안 A 시도 치환 OFF (§5.7 응답측 표기에 영향)
- `_HAS_SGG_REMAP=False` → T026 인천 치환 OFF (§5.6 · §6.3 코드 일치율에 하방 영향)
- `_RI_EMDS=frozenset()` → A-5 관측 경고 비활성 (오염을 알릴 마지막 신호마저 침묵)

**정정의 결정적 실측 (T047 C0-2 · C0-3).** T046 실패군(§5.5 의 1 km 초과 1,334 건) 전수를
현행(사전 정상) 런타임에 재질의한 결과와 대조군:

| 지표 (b_code 축) | T046 기록 | 현행 재질의 |
|---|---:|---:|
| 실패군 1,334 건 top-1 `b_code` 일치 | **0.00%** (전건 `bcode_eq=false`) | **99.92%** (1,332/1,333) |
| 실패군 "정답 보유하고도 밀림" | 84.18% (거리축·직접 비교 불가) | **0.00%** (0/1,333) |
| 대조군 1,380 건 (T046 성공군) | 100.00% | **100.00% — 회귀 없음** |
| top-1 리 문자열 채움 (실패군) | 0.00% | 99.92% |

> **헤드라인은 동일 축으로만 읽어라**: `b_code` 일치 **0% → 99.92%**. T046 의 84.18% 는
> 거리축(상위 5 에 25 m 내 정답), 현행 0.00% 는 b_code 축이라 직접 비교가 불가하다 —
> VWorld 좌표가 보존되지 않아 축 통일은 불가능하다(T047 검토 확정).

**오염 판별법** (메모리 `geocode-boot-race-lawd-ri-failopen` 와 동일): ① 부팅 로그에서
`lawd_ri: present` 3 줄 부재(가장 확실) → ② 역방향 `structure.ri` 전건 `None` 인데
`b_code[8:10]` 은 정상. ※ "우리 리 코드 42.58% vs VWorld 42.59% 일치"는 **보조 논거일 뿐**
결정적 지문이 아니다 — 리 코드는 `parcel.pnu` 에서 직접 오므로 `_HAS_LAWD_RI` 와 무관하게
항상 정상이다(T047 검토 권고 8).

#### 0.0.1 오염된 결론 — 철회 또는 재해석

| 위치 | 원 결론 | 정정 |
|---|---|---|
| §5.5 · §9 · §10.1 | "농촌 지번의 리 식별 실패", 순위 결함 +18.9 %p 회수 여지 | **철회.** 리 좁힘이 꺼진 측정 아티팩트. 현행 재질의 99.92%, 밀림 0% |
| §6.4 · §6.7.5 · §9 | "리 문자열 렌더링 누락 — 렌더링 자체의 결함" | **철회.** `ri_nm` 미조회(OFF) 아티팩트. 코드에 렌더링은 구현돼 있었다 |
| §7.7 해석부 | "우리 순방향이 그 123 건에서 리를 틀린다" | **철회.** 그 top-1 은 오염 런타임의 출력. 단 **절단 조인 결함 실증·수선 자체는 유효** |
| §5.7 응답측 표기 | 8092 응답이 `전라남도`/`광주광역시` | 안 A 치환 OFF 아티팩트. 단 **DB 실측(46/29 분포 · `parcel_12` 부재)은 유효** |
| §5.6 · §6.3 일부 | 법정동코드 일치율 (지번 70.63% 등) | **하방 오염.** 전남광주(치환 OFF)·인천(T026 OFF) 건이 불일치로 계상됨. 참값은 이보다 높다 |
| §10.1 1 순위 | 랭킹에 리 가중치 도입 | **표적 부존재** — T047 이 P0(부팅 방어)·P1(부존재 질의)로 재정의해 완료 |

#### 0.0.2 유효한 결론 — 오염과 무관

- **도로명층 전체**: 응답률 99.92% · 100 m 통과 96.14%(가중 97.55%) · 1 km 초과 0.15% —
  리 좁힘의 영향권 밖(도로명 검색은 리 사전과 무관)
- **도시 지번 1 km 초과 0/3,163** — 리가 없는 층이라 오염 축과 무관
- **§8 표본 B(부존재 지번 응답 행태)** — 결함 실재. T047 P1 이 수선(비주소 top-1 93→0%)
- **§7.6 오라클 구 시도코드 보정 · §7.7 절단 조인 결함 실증과 수선** — 측정계 자체의 결함, 유효
- **§9 의 상용 대비 위치 중 도로명 판정** — "도로명은 상용과 실질 대등" 유지
- **§5.7 의 DB 수준 실측** — 202605 세대 DB 가 전남광주 통합 미반영이었다는 사실 자체.
  ※ 단 **2026-08-28 T045 재빌드(202607 원천)로 geocode.sqlite 측은 해소됐다** — 신 산출물은
  `전남광주통합특별시`(코드 12 계열)·인천 신구(검단 28290 · 서해 28275 · 영종 28155 ·
  제물포 28125, 구코드 잔존 0)를 원천 수준에서 싣는다. PostGIS(8092)는 T049 재적재 전까지 구세대다

#### 0.0.3 재측정 설계 (T050 §3 — 범위 결정)

**시기.** T049(PostGIS 202607 재적재) **완료 후에만** 착수한다. 두 가지가 이미 바뀌었다 —
런타임 수선(T047 P0·P1)과 데이터 세대(202605→202607, T045). 지금 재측정하면 곧 폐기될
202605 적재본을 재는 것이고, T049 후 재측정은 **T046 의 재현이 아니라 신세대 기준 신규
측정**이다(런타임 수선과 세대 교체가 교락해 T046 수치와의 차분 해석은 불가함을 명기한다).

**게이트 (필수 선행 — 이행 확인됨).** `measure.py` 의 `dict_gate` 가 표본 적재·외부 호출
**전에** `/health` 의 `dict.lawd_ri` 를 기계 판독해 `present` 가 아니면 exit 3 으로 중단하고
(`--allow-dict-degraded` 강행 시 오염 표시가 `run_*.json` 에 남는다), `dict_same` 이 측정창
양끝의 사전 상태 불일치(측정 중 재기동)를 탐지한다. T047 이 구현했고 시험 23 케이스가 통과한다(`scripts/t046/test_dict_gate.py`, 2026-08-28 실행 확인).
md5·mount_source·기동시각·health 를 측정창 양끝에 기록하는 T046 관행은 유지한다.

**범위 (오염축 국소 재측정 + 회귀 방어).**

| # | 축 | 표본 | 재는 것 | VWorld 예산 |
|---|---|---|---|---|
| 1 | 순방향 농촌 지번 | 신세대 재추출 (§3.1 층 정의 유지, 규모 2,782 상당) | 1 km 초과율·b_code 일치 — §5.5 의 참값 | 소요 (일 한도 내 분할) |
| 2 | 역방향 리 4 지표 | 60 층 §6.7 계약 유지 (`src`·`fwd` 두 기준) | 리 문자열 채움률 — §6.4/§6.7.5 참값 | **0** (역방향 무한도) |
| 3 | 전남광주·인천 코드축 | 해당 시도 층화 표본 | 신코드(12·인천 신구) 반영 후 일치율. relax12 는 **불발동이 예상값** | 소량 |
| 4 | 회귀 방어 | 도시 지번·도로명 축소 표본 (각 ≥500) | §0.0.2 유효 결론이 신세대에서도 유지되는가 | 소량 |

전량(12,000 건) 재측정은 하지 않는다 — 유효 축(§0.0.2)까지 다시 재는 것은 예산 대비 정보
이득이 없고, 이 보고서의 역할은 정정 주해로 보존된다. 상세 표본 산정은 착수 태스크에서.

---

### 0.1 지번층 `source=parcel` 구간은 독립 비교가 아니다

태스크 §0 은 세 축(질의 주소·기대 좌표·측정값)이 각각 다른 출처라고 선언했다.
**이 선언은 지번층의 `source=parcel` 경로에서 성립하지 않는다.**

우리 지오코더의 지번 좌표는 연속지적도 필지의 대표점(`ST_PointOnSurface(parcel.geom)`)이고,
VWorld 지번 좌표도 같은 지적 원천에서 나온다. 예열 측정에서 **59 건 중 55 건이 1 cm 미만**으로
일치했고, 위도축에 **약 3.8 mm 의 고정 편이**가 관측됐다. 두 계통이 독립이라면 나올 수 없는 분포다.

따라서 **이 구간에서 재고 있는 것은 정확도가 아니라 좌표변환 일치도(EPSG:5179 → 4326)다.**
이 보고서는 다음 규칙을 지킨다.

- 모든 좌표 지표를 **`source` 별로 분해**해서 싣는다(§5.5). 합산값만 보고 판단하지 마라
- **"지번층 정확도 100%" 같은 표현을 쓰지 않는다.** 그렇게 읽힐 문장도 쓰지 않는다
- 우열이 실제로 갈리는 지점은 좌표 정밀도가 아니라 **커버리지·근사매칭·후보순위**이며,
  이 보고서의 판단 근거는 §7 실패 분류와 §5.3 임계 통과율에 둔다

### 0.2 기준이 하나뿐이다

기준값 계통은 VWorld 단독이다(Google 배제 근거는 §1.3). 기준이 하나이므로
**"우리가 맞고 VWorld 가 틀렸다"는 판정을 이 측정만으로는 낼 수 없다.**

분류 7(VWorld 측 의심)에 해당하는 건은 원천 레코드 대조나 지도 육안 확인 같은 **별도 근거**가
있어야 확정된다. 이번 측정에서 그 별도 근거 수집은 수행하지 않았다(§7.4). 따라서 해당 건들은
**전부 "판정 불가"로 남긴다.** 우리 쪽에 유리하게 계상하지 않는다.

### 0.3 역방향 주소축은 측정 도중에 고쳤다 — 표본 A 의 앞 2/3 는 여전히 공백이다

이것은 설계상의 제외가 아니라 A 단계 구현의 **결함**이었고, B 단계 측정 중에 발견해 수선했다.
그래서 표본 A 는 **두 개의 스키마 판**으로 나뉜다. 이 사실을 모르고 역방향 수치를 읽으면 안 된다.

**결함.** 계획 §4.2 는 역방향 비교 대상을 **우리 `/reverse` 응답의 `address.structure`** 로 못박고,
**"`nearest[]` 는 쓰지 않는다"** 고 명시했다. 구현(v1)은 정확히 반대로 했다.

| 계획 §4.2 | v1 구현 | 결과 |
|---|---|---|
| `address.structure` 를 읽어라 | `body["results"][0]…` — **우리 `/reverse` 에 없는 키** | 전건 `None`. 시도·시군구·읍면동·**리** 계층 일치율 전부 산출 불가 |
| `nearest[]` 는 쓰지 마라 | `body["nearest"][0]["dist_m"]` 를 읽음 | 유일하게 채워진 역방향 수치가 **계획이 배제한 것** |
| `address_source` 별로 층을 나눠라 | 필드를 기록하지 않음 | 합성(`knn`)·실주소(`pip_key`) 분리 불가 |

같은 오독의 양면이다 — 순방향 `/geocode` 의 응답 형태(`results[]`)를 역방향에 그대로 적용했다.
**필요한 데이터는 양쪽 응답에 실제로 들어 있었다.** 읽지 않았을 뿐이다(§6.1 실증).

**수선(스키마 v2).** 판정 레코드에 **추가만** 했다 — v1 필드의 이름·의미·산출식은 하나도 바꾸지
않았으므로 순방향 지표는 전 구간 동일 기준으로 합산된다. 추가된 것은 역방향 계층 일치
(시도·시군구·읍면동), 리 4 지표(채움률·코드 일치·문자열 일치·코드-문자열 상호 불일치),
완화 병기, `address_source` 다. 주소 문자열은 저장하지 않고 **판정 불리언만** 남긴다(§3.3 준수).
A 단계 테스트 278 개는 수선 후에도 전부 통과한다.

**스키마 경계(정확히 기록한다).**

| 구간 | 판정 레코드 | 층 | 시도 | 역방향 주소축 |
|---|---|---|---|---|
| v1 | 표본 A 1 ~ **8,000** 번째 | 40 층 | 10 시도 | **공백** |
| v2 | 표본 A **8,001** ~ 12,000 번째 | 20 층 | 6 시도 | 측정됨 |

경계는 청크 경계와 정확히 일치한다. 15:03:07 에 교체했고 `resume: done=8000 → start=8000
(재소모 0 건)` 이었다 — 중복도 손실도 없다.

**v2 구간은 무작위 부분표본이 아니다.** 표본 파일이 층 블록 순서(층당 200 건 연속)라서
v2 는 사전순 뒤쪽 6 시도(`jeju`·`jeonbuk`·`jeonnamgwangju`·`sejong`·`seoul`·`ulsan`)에만 걸리고
**v1 과 겹치는 층이 0** 이다. 따라서 역방향 주소축 수치는 **전국 추정치가 아니다.**

| v2 커버리지 | 값 |
|---|---|
| 층 | 20 / 60 |
| 모집단 ΣN_h | 4,941,058 / 17,141,159 = **28.83%** |
| 지번층 / 도로명층 | 29.43% / 28.47% (층위 균형은 유지) |
| 농촌 층(리 산출 가능) | 8 층, ΣN_h 2,583,318 |
| 시도코드 12(전남광주통합) 층 | **포함** — T042/T043 완화 축이 역방향에서도 측정된다 |

역방향 주소축의 모든 비율은 **이 20 층 안에서만** 사후가중한다. 전국 값으로 인용하지 마라.

**v1 구간은 영구히 복구 불가다.** 역방향 질의점(VWorld 순방향 좌표)이 판정 레코드에도 진단
레코드에도 저장되지 않아, 우리 쪽만 재조회해 되살릴 수 없다. 되살리려면 순방향부터 다시 호출해야 한다.
(재확인 실측 — `verdict_main.jsonl` 30 키에 좌표 없음, `verdict_warmspread.jsonl` 도 같은 키 집합,
`~/geocode-build/t046/diag/` 에는 로그만 있고 `diag_*.jsonl` 이 **아예 없다**. `--diag` 를 쓰지
않았기 때문이다.)

#### 0.3.1 C 단계 재측정 — 지표는 냈다. 다만 **질의 좌표 기준이 다르다**

역방향 `getAddress` 에는 **호출 한도가 없다**(계획 §1.1). 순방향 예산(40,000/일)을 한 건도
쓰지 않고 역방향만 다시 돌릴 수 있다. C 단계는 그렇게 했다 — 표본 A 12,000 건 전량,
**60 층 전부**, `type=both`.

| | B 단계 (v2 구간) | C 단계 (rev2) |
|---|---|---|
| 층 | 20 / 60 | **60 / 60** |
| 모집단 몫 | 28.83% | **100%** |
| 시도·시군구·읍면동·리 | 산출 | 산출 |
| **지번 본번·부번·산** | 없음 | **산출** |
| **도로명** | 없음 | **산출** |
| **건물번호 본번·부번** | 없음 | **산출** |
| 정규화 전후 병기 | 완화(시도코드)만 | **축마다 3 단**(원문·bare·정규화) |
| `address_source` 분리 | 20 층 | **60 층** |
| 질의 좌표 | VWorld 순방향 좌표 | **표본에서 재유도**(아래) |

**질의 좌표를 원본과 동일하게 맞추는 것은 불가능했다.** 그 좌표가 없고 순방향 재호출은
금지이기 때문이다. 태스크가 허용한 "표본에서 재유도"를 택하되, **어느 기준도 중립이
아니라는 사실을 숨기지 않기 위해 두 기준을 모두 돌려 참값을 사이에 가뒀다.**

| 기준 | 좌표의 출처 | 성질 |
|---|---|---|
| `src` 도로명 | 원천 202607 의 **출입구 좌표**(없으면 건물중심) | **가장 중립적.** 점을 만든 것이 어느 지오코더도 아니다 |
| `src` 지번 | 우리 `parcel` 폴리곤의 `ST_PointOnSurface` | **우리에게 자기참조.** 우리 `/reverse` 의 `pip_key` 경로가 같은 필지를 다시 문다. 실측 일치율이 사실상 항진명제 수준이다(§6.7). **상한으로만 읽어라** |
| `fwd` 양 층 | 우리 8092 순방향 top-1 | 위 항진명제는 없다. 다만 **편향 방향은 확정할 수 없다**(아래) |

**`fwd` 를 "보수적"이라고 부르지 않는다.** 그렇게 쓰고 싶었지만 성립하지 않는다.
이 측정은 *같은 점 P 에 대해 두 서비스가 같은 주소를 말하는가*를 묻는데, 순방향이
틀리면 P 가 다른 곳으로 옮겨갈 뿐 **양쪽 모두 여전히 같은 P 를 설명한다.** 따라서
순방향 오차가 역방향 불일치로 자동 전이되지 않는다. `fwd` 가 재는 것은
**왕복 일관성**이고, 우리 순방향이 고른 점들의 분포 위에서 잰다는 것이 `src` 와의
차이다. 그 분포가 일치율을 올릴지 내릴지는 이 측정으로 알 수 없다 — **미확인이다.**

원본 기준(VWorld 순방향 출력)도 중립이 아니었다 — **VWorld 자기 출력에 VWorld 역방향을
거는 구조**라 VWorld 쪽에 자기참조였다. `src` 도로명 기준은 그보다 낫다.

**따라서 rev2 수치는 §6.3·§6.4 의 `rev_*` 수치와 직접 비교할 수 없다.** 기준이 다르다.
같은 표에 나란히 놓지 않는다.

**완료조건 2 재판정은 §6.8 에 있다.** "부분 충족"으로 뭉개지 않는다.

### 0.4 미실행·미측정 항목 일람

계획 §8 이 리포트 필수 기재로 못박은 19 항목 중 아래는 **실행되지 않았거나 부분 실행**이다.

| § 8 항목 | 상태 | 사유 |
|---|---|---|
| 14 역방향 도로명 예비조사(200 건) | **C 단계에서 실행** — 예비조사 200 건이 아니라 **본 측정 12,000 건**으로 | A·B 단계는 `type=PARCEL` 고정이라 미실행이었다. C 단계가 `type=both` 로 바꿔 편입했다(§6.5·§6.7) |
| 15 역방향 `address_source` 분리 | **C 단계에서 60 층 전체** | B 단계는 v2 구간(20 층·모집단 28.83%)뿐이었다(§6.7) |
| 16 재호출 일치율 부록(100 건) | **미실행** | `measure.py` 에 재호출 모드가 없다. C 단계도 만들지 않았다 — F1 범위 밖이고, 역방향 재측정이 사실상 부분적 재현성 확인 역할을 한다(§6.7 의 `our_addr_count` 대조는 하지 않았다. 미확인이다) |
| 13 분류 7·8 육안 대조 | **대상 0 건** | 두 분류 모두 관측 0 건이라 대조할 것이 없다(§7.4). 도달가능성 테스트는 통과했으므로 "설계상 불가"가 아니라 "이 표본에서 미관측"이다 |

역방향 계층·리 지표(§8 항목 15 와 같은 구간)는 **C 단계에서 60 층 전체로 확대**됐다(§0.3.1).
다만 질의 좌표 기준이 원본과 다르다 — 그 사실과 편향 방향은 §0.3.1 에 있다.

### 0.5 오라클에도 결함이 있었다 — 다만 분류표는 바뀌지 않았다

B 단계 중 오라클(존재 판정)이 강원·전북 표본 **1,560 건(13.00%)** 을 "우리 DB 에 없음"으로
오판하고 있음을 확인했다. 원천은 구 시도코드(42·45)를 쓰는데 우리 DB 는 현행(51·52)으로
적재돼 있어서다. 접두를 치환하면 **99.10% 가 회수된다** — 실제로는 우리 DB 에 있는 주소였다.

**그러나 사후 재계산 결과 분류 이동은 0 건이다.** 오라클은 무응답(A 군)에서만 분류를 가르는데
A 군이 전 표본 6 건뿐이고, 문제의 1,560 건은 전부 응답이 있어 분류 5·6·11 에 있었다.
"커버리지 결손으로 우리 결함이 면책된다"는 경로는 **이 표본에서 발생하지 않았다.**
바뀌는 것은 오라클 분포(A/P/N) 자체이므로, "우리 DB 에 없는 주소가 N%" 형태의 진술을
보정 전 수치로 하면 틀린다. 상세는 §7.6.

#### 0.5.1 C 단계 정정 — 보정은 **두 축에만** 걸려 있었다

위 문단과 §6.6 · §12.5 는 이 보정을 "완료"로 적었다. **그 표기는 틀렸다.** 보정이 걸린 것은
지번·도로 축뿐이고 **심판 축과 본번 근사 축에는 없었다.** 검수가 실측한 대로 분류 11 의
72 건 중 **22 건(30.6%)** 이 이 결함에 기인한다. C 단계에서 두 축까지 확장했고, 경위·전후
수치·편향 방향은 **§7.6.1** 에 있다. 방향은 전부 **우리에게 불리하다** — 보정하지 않은 쪽이
우리에게 유리했다.

같은 파일을 다시 읽다가 **별개의 결함**을 하나 더 찾았다. 오라클 조회 키가 법정동코드
10 자리에서 **리 2 자리를 잘라** 이웃 리의 동일번지 필지를 존재·심판 근거로 세고 있었다.
**분류 9 로 보고한 123 건은 전부 이 산물이며, 수선하면 0 건이 된다.** 실증은 **§7.7**.
이 결함은 검수가 지적한 것이 아니라 C 단계가 스스로 찾은 것이고, 역시 우리에게 불리하다.

미실행 항목들은 **새 계측기를 작성해야** 실행 가능하다. 278 개 테스트로 검증된 기존 파이프라인과
달리 검증되지 않은 계측기가 낸 숫자는 근거로서 더 약하므로, 숫자를 만들어 채우는 대신
**공백을 공백으로 보고하고 T047 수선 명세를 남기는 쪽**을 택했다 — 이것이 A·B 단계의 판단이었다.
**C 단계는 그 판단을 뒤집었다.** 역방향은 호출 한도가 없고 `type=both` 한 번이면 지번·도로명이
함께 오므로, 순방향 예산을 한 건도 쓰지 않고 누락 지표를 실제로 산출할 수 있었다. 결과는
§6.7 이다. 계측기는 새로 쓰되 **테스트 85 개를 함께 붙였다**(§7 실행 결과).

---

## 1. 측정 대상과 배제 결정

### 1.1 측정 대상 = 8092 PostGIS 판

| 항목 | 값 |
|---|---|
| 컨테이너 | `server-geocode-pg-1` |
| 엔드포인트 | `http://127.0.0.1:8092` |
| 지오코더 소스 md5 | `5bb1c78df04c9f33d9a2c396a240e307` (`geocode-api-pg.py`) |
| 적재 규모 | `places` 16,282,127 · `areas` 8,929 |

**이 컨테이너에는 healthcheck 가 정의되어 있지 않다.** 그래서 "측정 중에 대상이 바뀌지 않았는가"를
컨테이너 상태로 확인할 수 없고, **마운트 소스와 md5 대조로 대체**했다(§2).

컨테이너가 물고 있는 소스는 이 워크트리가 아니라 `.worktrees/geocode-none-fix/` 다. md5 가
기준값과 일치하므로 측정 대상 동일성에는 문제가 없으나, **이 워크트리의 지오코더 코드를 고쳐도
8092 에는 반영되지 않는다.** 이 태스크는 측정 전용이므로 지오코더 코드는 손대지 않았다.

### 1.2 8082(SQLite 판) 제외 — 관측표 전문

컨테이너 `server-geocode-1` (`127.0.0.1:8082`), md5 = `a8097ada43f1a1e4b6506c2c1e797475`, 상태 healthy.
마운트 DB = `geocode/geocode.sqlite`, **120,737,792 바이트(6 월 16 일자)**.
테이블 목록은 `places / places_fts* / place_rtree* / areas / area_rtree* / meta` 뿐 — **주소 테이블이 없다.**

실측 질의 결과(계획 §1.7 관측표 전문):

| 질의 | 8092 | 8082 |
|---|---|---|
| 서울특별시 종로구 세종대로 175 | 결과 있음 | `results: []` |
| 충청남도 예산군 덕산면 사천리 200-1 | 결과 있음 | `results: []` |
| 충청남도 예산군 덕산면 사천리 200-3 | — | `results: []` |

**결정: 8082 는 §5–§7 지표 측정에서 제외한다.** 근거 셋이다.

1. 물고 있는 것이 전국 주소 DB 가 아니라 **POI 전용 축소 아티팩트**다. 여기서 나올 "응답률 ≈ 0%"는
   구현 품질이 아니라 **적재 상태**를 재는 숫자이고, "상용 대비 우리 위치"에 아무 답도 주지 않는다
2. 응답 계약이 다르다(`contract_version` 없음). `structure` 축 지표를 애초에 산출할 수 없다
3. VWorld 순방향 예산은 공유 자원이다. 답이 무의미한 대상에 쓸 이유가 없다

**다만 위 표 자체는 사실로 남긴다** — "SQLite 판은 현재 전국 주소를 담고 있지 않다"는 것은
그 자체로 보고할 가치가 있는 관측이다. 재적재 후의 비교는 별도 태스크의 몫이다.

### 1.3 Google 배제 근거

**기준은 VWorld 단독이며 Google 은 아래 근거로 배제했다**(태스크 §1.2 의 실증).

- Google Geocoding 이 한국 지번 질의에 대해 **Plus Code** 를 반환하는 사례가 확인됐다.
  Plus Code 는 좌표 인코딩이지 주소가 아니므로 주소 계층 비교의 기준값이 될 수 없다
- Google 의 한국 지도 데이터는 **TMap Mobility 제공분**이다. 독립 계통으로 취급할 근거가 약하고,
  약관상 결과 저장·재배포 제약이 VWorld 보다 강하다
- 태스크 §1.2 는 이 배제를 **"다시 검토하지 마라"**로 확정했다

Google Maps 스크래핑이나 API 키 확보 시도는 하지 않았다(태스크 §6 절대 금지).

---

## 2. 측정 창

healthcheck 부재(§1.1)를 md5 대조로 대체한 기록이다.

모든 창에서 마운트 소스는 `.worktrees/geocode-none-fix/server/geocode-api-pg.py`,
호스트·컨테이너 md5 는 **`5bb1c78df04c9f33d9a2c396a240e307`**(기준값과 일치),
컨테이너 ID 는 geocode `9ae219f7c168` · PostGIS `101083cafd54` 로 **전 구간 동일**하다.

| 창 | 시작 | 종료 | 건수 | `window_same` |
|---|---|---|---|---|
| 예열 (warmup) | 12:55:43 | 12:56:21 | 100 | True |
| 예열확산 (warmspread) | 12:5x | 12:59 | 120 | **기록 없음** (아래) |
| 본측정 A · 스키마 v1 | **14:15:03** | 14:58:44 | 8,000 | **기록 없음** (아래) |
| 본측정 A · 스키마 v2 | **15:03:07** | 15:21:38 | 4,000 | True |
| 표본 B (perturb) | 15:21:51 | 15:33:26 | 1,200 | True |
| **역방향 재측정 · 기준 `src`** (C 단계) | **18:05:57** | 18:56:59 | 12,000 | **True** |
| **역방향 재측정 · 기준 `fwd`** (C 단계) | **18:57:06** | 20:02:27 | 12,000 | **True** |

C 단계 두 창도 마운트 소스·호스트/컨테이너 md5·컨테이너 ID 가 위 기준값과 **전부 동일**하다
(`run_rev2_src.json` · `run_rev2_fwd.json` 의 `window_start`/`window_end`).

**`/health` 관측(정직성 기록).** `fwd` 창은 시작·종료 프로브가 **모두 `ERR:HTTP Error 503`**
이었다. `window_same` 비교 항목에 `health` 는 들어가지 않으므로 판정에는 영향이 없으나,
사실 자체를 남긴다. 같은 시각 `/geocode`·`/reverse` 를 직접 호출하면 HTTP 200 에 완전한
응답이 왔고, 측정 종료 직후 `/health` 는 **0.71 s 에 200**(`places 16,282,127` — `src` 창
시작값과 동일)으로 회복했다. `/health` 가 `COUNT(*)` 를 도는 탓으로 보이며, 아래 재기동
직후 현상과 같은 계열이다. 상세는 §6.7.8.

**창 기록이 없는 두 구간.** `run_*.json` 은 **완주 시점에만** 기록된다.
warmspread 는 `window_start`/`window_end` 가 `None` 으로 남았고, 본측정 A 의 v1 구간은
스키마 교체를 위해 완주 전에 중단했으므로 종료 기록을 남기지 못했다.
다만 **교체 직후 재개 시점(15:03:07)의 시작 기록이 v1 종료 시점의 대조 역할을 한다** —
컨테이너 ID·마운트·md5 가 모두 기준값과 같았다. 그래서 대상 동일성 자체는 전 구간 확인된다.
소실된 것은 "창 단위 종료 도장"이라는 형식이지 대조 사실이 아니다.

**컨테이너 재기동 관측(정직성 기록).** A 단계는 `geocode` 컨테이너의 기동 시각을
`2026-08-20T17:05:04Z` 로 기록했으나, B 단계 착수 시점의 `docker inspect` 는
`StartedAt = 2026-08-21T04:45:26Z`, `RestartCount = 0` 을 보였다. 즉 **B 단계 착수 약 27 분 전에
컨테이너가 다시 올라왔다.** 컨테이너 ID·마운트 소스·소스 md5·적재 규모가 모두 그대로이므로
측정 대상 아티팩트는 동일하다고 판단했으나, 관측 사실 자체를 숨기지 않고 여기 남긴다.

착수 직후 `/health` 첫 호출이 statement timeout 을 반환했고 재시도 2·3 회차에서 정상 응답했다.
실제 `/geocode` 질의는 그사이에도 성공했다. 재기동 직후 `COUNT(*)` 의 콜드 캐시 현상으로 판단했다.

---

## 3. 표본 설계와 재현

### 3.1 층 정의와 크기

16 시도 × {도로명, 지번} × {도시, 농촌} = 명목 64 층.
그중 4 층이 원천에 존재하지 않아(`daejeon|rural|{jibun,road}`, `seoul|rural|{jibun,road}`)
**실효 60 층**이다. 층당 200 건 균등배분 → **표본 A = 12,000 건**.

교란 표본 B 는 지번 30 층 × 40 건 = **1,200 건**(§8).

| 항목 | 값 |
|---|---|
| 모집단 ΣN_h | 17,141,159 |
| 최소 층 | `sejong\|urban\|jibun` N_h = 1,899 |
| 최대 층 | `gyeongbuk\|rural\|road` N_h = 1,152,387 |
| 제외 — 지하 | 3,560 |
| 제외 — 대표성 없음 | 1,770,131 |
| PNU 불일치(기록만) | 2,331 |

**표본 A·B 중복 = 0 건**(`pnu` 기준·`query` 기준 양쪽에서 확인).

**표본 A 내부 중복(정직성 기록).** 12,000 건 중 고유 `query` 는 11,952 건이다.
40 종의 질의 문자열이 2 회씩 뽑혀 **초과 48 건**이 발생했다. 전부 **같은 층 안**에서 발생했고
지번/도로명 교차 중복은 없다. 원천에 동일 주소 문자열이 여러 행으로 존재(세대 단위 행 등)하고
저수지 표집이 행 단위로 동작한 결과다. 유효 독립 질의 수는 12,000 이 아니라 **11,952** 이다.
`pnu` 기준 중복 76 종 중 34 종은 같은 필지를 지번층·도로명층에서 각각 뽑은 것으로 설계상 정상이다.

### 3.2 시드와 파생식

| 항목 | 값 |
|---|---|
| 마스터 시드 | `20460821` |
| 층 시드 | `int(sha256(f"{20460821}:{sido}:{urban}:{atype}").hexdigest()[:16], 16)` |
| 추출 | 1-pass 저수지 표집 (k=200) |
| 읽기 순서 | 파일명 사전순 오름차순, 파일 내부는 물리적 행 순서 |
| 인코딩 | `cp949`, 디코드 치환 행 0 |

### 3.3 원천 파일 32 개

행안부 202607 내비게이션용DB (`staged/navi`), **32 파일 / 4,061,250,503 바이트 (3.78 GiB)**.
집계 해시 **`9d0fc6ac167d1f07bd4bc9d1e811defdf4b6c84151cf2bf81b8e73c7d7b28b5a`**.

파일별 경로·바이트·SHA-256 전문은 §12.6 부록에 싣는다.

### 3.4 균등배분의 비용 — 숨기지 않는다

층별 균등배분(200 건)은 모집단 비율과 크게 어긋난다. 그 통계적 대가는 다음과 같다.

| 지표 | 설계 시점(60 층) | 지번층 실측 | 도로명층 실측 |
|---|---|---|---|
| 층수 H | 60 | 30 | 30 |
| 가중치 최대/최소 | — | 290.7 배 | 254.7 배 |
| ΣW_h² | 0.05824361 | 0.05585423 | 0.06124779 |
| 설계효과 `deff` | **1.7473** | 1.6756 | 1.8374 |
| 유효 표본 | **3,434 / 6,000** | 3,581 | 3,265 |
| 가중 CI 반폭 | ±1.672 %p | ±1.638 %p | ±1.715 %p |

**즉 12,000 건을 측정했지만 통계적 정밀도는 단순무작위 6,848 건에 해당한다.**
이 보고서의 모든 가중 비율에는 위 CI 를 적용해 읽어야 한다.
유한모집단 보정(FPC)은 계산했으나 **적용하지 않았다**(최소 층에서도 0.946, 대부분 0.99 초과라
보정이 CI 를 좁히는 방향으로만 작용해 보수적 쪽을 택했다).

### 3.5 재현성의 한계 — 시드 고정만으로는 재현되지 않는다

A 단계 §5-4 가 규명한 사실이다. **층 시드가 층 이름만으로 결정되므로, 제외 규칙이 바뀌면
읽기 스트림이 갈려 같은 시드에서도 다른 표본이 나온다.**

재현하려면 다음 **세 가지가 모두** 같아야 한다.

1. 마스터 시드 `20460821`
2. 제외 규칙(지하 3,560 / 대표성 없음 1,770,131 의 판정 기준 포함)
3. 원천 집계 해시 `9d0fc6ac167d1f07bd4bc9d1e811defdf4b6c84151cf2bf81b8e73c7d7b28b5a`

"시드를 적어뒀으니 재현된다"고 쓰는 것은 사실이 아니다.

---
## 4. 호출 집계와 실패 처리

### 4.1 VWorld 호출 총계

| 구분 | 건수 |
|---|---|
| 순방향 (표본 A) | 12,000 |
| 역방향 (표본 A) | 11,913 (VWorld 순방향이 `OK` 인 건에만 건다) |
| 순방향 (표본 B) | 1,200 (표본 B 는 역방향을 돌리지 않는다) |
| **VWorld 순방향 합계** | **13,200** |
| 일일 한도 | 40,000 (순방향). **역방향은 한도 없음** |
| 한도 도달 | **없음** (`over_limit = false`) |

### 4.2 `status` 분포 — 부재와 전송실패를 구분한다

`status != "OK"` 는 HTTP 200 이어도 실패로 계산한다. 부재를 성공으로 세지 않는다.
그리고 **부재와 전송실패는 성격이 다르다** — 전자는 VWorld 의 사실이고 후자는 측정 인프라의 결함이다.

| VWorld 순방향 (12,000) | 건수 | 비율 | 성격 |
|---|---:|---:|---|
| `OK` | 11,913 | 99.28% | — |
| `NOT_FOUND` | 52 | 0.43% | **VWorld 의 사실** — 기준값 부재 |
| `NETWORK` 18 · `HTTP_502` 16 · `TIMEOUT` 1 | 35 | 0.29% | **측정 인프라 결함** — 전송 실패 |

| VWorld 역방향 (11,913 호출) | 건수 | 비율 |
|---|---:|---:|
| `OK` | 11,886 | 99.05% |
| `NOT_FOUND` | 4 | 0.03% |
| `HTTP_502` 11 · `NETWORK` 11 · `TIMEOUT` 1 | 23 | 0.19% |
| (호출 안 함 — E1 게이트) | 87 | 0.72% |

### 4.3 게이트

| 게이트 | 정의 | 건수 |
|---|---|---|
| **E1** | VWorld `status != OK` | **87** (= 부재 52 + 전송실패 35, 정확히 일치) |
| **E2** | 우리 8092 의 5xx·타임아웃 | **0** |
| **D0** | 유효 분모 = 12,000 − 87 − 0 | **11,913** |

층별로는 지번 E1 54 / D0 5,946, 도로명 E1 33 / D0 5,967 이다.

### 4.4 재시도 — **앞 구간 계수기는 소실됐다**

`run_main.json` 은 재개할 때마다 덮어써진다. 첫 구간(8,000 건)은 완주 전에 스키마 교체로
중단됐으므로 **그 구간의 재시도·지연 계수기는 기록되지 않았다.** 아래는 **마지막 구간
4,000 건만**의 값이다. 전체 값으로 읽으면 안 된다.

| 항목 (마지막 4,000 건 구간 한정) | 값 |
|---|---|
| 재시도 `vworld\|fwd` / `vworld\|rev` / `ours\|rev` | 8 / 14 / 11 |
| 재시도 성공 | 8 / 14 / 11 (전건 성공) |
| 지연 `vworld\|fwd` 평균·p95 | 0.109 s · 0.266 s |
| 지연 `ours\|fwd` 평균·p95 | 0.027 s · 0.114 s |
| 지연 `ours\|rev` 평균·p95 | 0.205 s · **0.786 s** |

`status` 분포·게이트·호출 건수는 판정 레코드 12,000 행에서 **전량 복원**되므로 §4.2·§4.3 은
전 구간 값이다. 소실된 것은 재시도 횟수와 지연 분포뿐이다.

제어 규약: 동시성 4, 초당 8 요청 토큰버킷, 네트워크·5xx·타임아웃에 한해 3 회 지수 백오프.
`NOT_FOUND` 같은 정상 응답은 재시도하지 않는다.
**`OVER_REQUEST_LIMIT` / HTTP 429 도달 시 즉시 전체 중단**이 규약이며, **이번 측정에서 한도에는
도달하지 않았다.**

### 4.5 측정 중 관측된 이상

우리 8092 `/health` 가 v2 구간 재개 시점에 **HTTP 503** 을 반환했다. 본문은
`PostGIS 연결 실패: canceling statement due to statement timeout` 으로, health 질의(1,628 만
행 카운트) 자체의 타임아웃이다. 같은 시각 `/geocode`·`/reverse` 실질의는 정상 응답했고,
컨테이너 로그에도 기동 시각(04:45:26) 이후 새 오류가 없다. 종료 시점 재검사에서는
`{"ok": true, "places": 16282127, "areas": 8929}` 로 정상이었다.
**서비스 중단이 아니라 health 엔드포인트의 결함**으로 판단해 측정을 계속했다.
이 컨테이너에 healthcheck 가 없다는 사실(§1.1)과 합쳐 T047 에 남긴다.

---

## 5. 순방향 지표 (§4.1)

분모 규약: 거리·임계 통과율 = **양측 응답**, 완전 실패 = **표본 전체**, 분류표 = **D0**.

### 5.1 응답률

| 층 | 엄격 (`kind=addr` ≥ 1) | 광의 (`results` ≥ 1) | 차이 = F2 |
|---|---:|---:|---:|
| 지번 | 99.98% | 99.98% | 0 건 |
| 도로명 | 99.92% | 99.97% | 3 건 (0.05%) |

**응답률 자체는 문제가 아니다.** 우리 지오코더는 거의 항상 무언가를 돌려준다.
문제는 그 답이 맞느냐이며, 그것이 §5.3 이하다.

### 5.2 좌표 오차 분위 (m, 비가중)

| 분위 | 지번 top-1 | 지번 상위5 최선 | 도로명 top-1 | 도로명 상위5 최선 |
|---|---:|---:|---:|---:|
| p50 | **0.004** | 0.004 | 7.985 | 7.985 |
| p75 | 0.041 | 0.004 | 16.757 | 16.757 |
| p90 | **4,272.258** | 0.038 | 43.135 | 43.135 |
| p95 | 5,962.071 | 0.041 | 81.529 | 80.609 |
| p99 | 9,289.572 | 3,543.186 | 260.823 | 259.656 |
| 최대 | 20,386.407 | 11,583.882 | 259,114.004 | 236,459.346 |

**지번 분포는 이봉(bimodal)이다** — 밀리미터 아니면 킬로미터다. 중간이 거의 없다.
p50 이 4 mm 인 것은 정확도가 아니라 §0.1 의 좌표변환 일치도다.
p90 이 4.3 km 로 튀는 것이 실제 결함이며, 그 정체는 §5.5 에서 규명한다.

도로명은 단봉이고 중앙값 8 m 다. 이 8 m 는 오차가 아니라 **앵커 정의 차이**(출입구 대
건물중심 대 VWorld 도로 기준점)로 보는 것이 타당하다.

### 5.3 임계 통과율

| 임계 | 지번 top-1 | 지번 가중 | 지번 상위5 | 도로명 top-1 | 도로명 가중 | 도로명 상위5 |
|---|---:|---:|---:|---:|---:|---:|
| ≤ 5 m | 76.35% | 71.68% | 96.28% | 26.48% | 24.48% | 26.48% |
| ≤ 25 m | 76.40% | 71.77% | 96.35% | 83.46% | 84.52% | 83.46% |
| ≤ 100 m | 76.42% | 71.81% | 96.37% | 96.14% | 97.55% | 96.16% |
| ≤ 1 km | 77.56% | 72.89% | 96.72% | 99.85% | 99.84% | 99.85% |
| 양측응답/표본 | 99.08% | | | 99.37% | | |

가중 CI 반폭은 지번 ±1.638 %p, 도로명 ±1.715 %p 다(§3.4).

**지번의 ≤5 m 와 ≤100 m 가 사실상 같다**(76.35% → 76.42%). 이봉 분포의 직접 증거다.
**상위 5 후보로 넓히면 96.28% 로 뛴다** — 20 %p 차이가 순위 문제의 크기다.

도로명은 ≤5 m 가 26.48% 로 낮지만 top-1 과 상위5 가 동일하다. 순위 문제가 없고,
앵커 정의 차이가 5 m 안에 들지 못할 뿐이다. ≤25 m 83.46%, ≤100 m 96.14% 가 실질 지표다.

### 5.4 완전 실패 (분모 = 표본 전체 6,000)

> **분모 규약(F5).** 이 표의 분모는 층 표본 전체 6,000 이고, **분자는 게이트 통과분(D0)
> 한정**이다. 무응답 실측은 `our_addr_count == 0` 이 지번 9 건이지만 그중 8 건은
> **VWorld 도 실패한 E1 건**이라 외부기준이 없어 배제했다. 배제 자체는 타당하나
> A 단계 보고서에 규약이 적혀 있지 않았다 — 여기서 명시한다.

| 구분 | 지번 | 도로명 |
|---|---:|---:|
| 무응답 (`kind=addr` 0 건, 분자는 D0 한정) | 1 (0.02%) | 5 (0.08%) |
| 1 km 초과 오답 | **1,334 (22.23%)** | 9 (0.15%) |
| **완전 실패 합** | **1,335 (22.25%)** | **14 (0.23%)** |
| (참고) top-1 = 상위5 최근접 | 77.26% | **99.88%** |

### 5.5 결함의 정체 — 농촌 지번의 리(里) 식별 실패

> **분모 규약(F5).** 아래 `순방향 b_code 일치` 열의 분모는 **양측응답 건수**이고
> 판정불가(한쪽 코드 부재) 17 건을 **불일치로 계상**한다. §5.6 은 판정불가를
> **제외**하므로 두 절의 규약이 다르다 — 도로명 농촌은 이 규약에서 89.68%,
> §5.6 규약에서 90.24% 다. 이 절 쪽이 **우리에게 불리한(보수적)** 방향이다.

여기가 이 측정의 핵심이다. 1 km 초과 오답을 도농으로 쪼개면 결과가 갈린다.

| 층 / 도농 | 양측응답 | 1 km 초과 | 그중 상위5 에 25 m 내 정답 있음 | 순방향 b_code 일치 |
|---|---:|---:|---:|---:|
| 지번 urban | 3,163 | **0 (0.00%)** | — | 91.78% |
| 지번 rural | 2,782 | **1,334 (47.95%)** | **1,123 (84.18%)** | **46.59%** |
| 도로명 urban | 3,190 | 6 (0.19%) | 0 | 91.19% |
| 도로명 rural | 2,772 | 3 (0.11%) | 0 | 89.68% |

세 가지가 동시에 성립한다.

1. **1 km 초과 오답은 전부 농촌 지번이다.** 도시 지번은 3,163 건 중 **0 건**이다
2. **그 1,334 건은 전건 `bcode_eq = False`** — 우리가 고른 필지가 **다른 법정동**에 있다
3. **역으로, 법정동코드가 맞은 4,199 건 중 1 km 초과는 0 건**이다

즉 좌표를 못 만드는 것이 아니라 **어느 리(里)의 필지인지를 못 고른다.** 같은 본번-부번을
가진 다른 리의 필지를 1 순위로 올리고, 정답은 상위 5 안에 있으면서 밀려 있다(84.18%).

이 결함은 §6.4 에서 독립적으로 관측된 것과 **같은 뿌리**다 — 우리 역지오코딩은 법정동코드에는
리를 담으면서 주소 문자열에서는 리를 통째로 누락한다. 순방향에서는 리를 식별키로 쓰지 못하고,
역방향에서는 리를 출력하지 못한다. **리가 우리 주소 파이프라인에서 일급 시민이 아니다.**

**이 발견은 §0.1 의 자기참조 한계에 무력화되지 않는다.** 오히려 그 반대다. 좌표 원천이 같아
좌표 정밀도로는 우열이 갈리지 않는 조건에서, **남은 것은 선택(selection)뿐이고 우리는 거기서
진다.** 자기참조 구간이기 때문에 이 결론이 더 강해진다.

> **[정정 2026-08-28 · T050]** 이 절의 핵심 결론을 **철회한다.** 1,334 건(전건 리 토큰 보유)을
> 현행(사전 정상) 런타임에 전수 재질의한 결과 top-1 `b_code` 일치 **0% → 99.92%**(1,332/1,333),
> "정답 보유하고도 밀림" **0.00%**, 대조군 1,380 건 회귀 없음(T047 C0-2·C0-3). 리 좁힘은
> 코드에 구현돼 있었고 측정 인스턴스에서만 꺼져 있었다(`_HAS_LAWD_RI=False`, §0.0).
> "리가 우리 주소 파이프라인에서 일급 시민이 아니다"는 판정은 성립하지 않는다.
> **유효하게 남는 것**: 도시 지번 1 km 초과 0/3,163 · 도로명층 수치 · 좌표 분위(§5.2).

### 5.6 법정동코드 일치 — 완화 전후 병기

| 층 | 엄격 | 엄격 가중 | 완화 후 | 완화 후 가중 | 완화가 뒤집은 건 |
|---|---:|---:|---:|---:|---:|
| 지번 | 70.63% | 64.42% | 72.43% | 66.61% | **107 (1.80%)** |
| 도로명 | 90.75% | 86.06% | 93.12% | 89.81% | **141 (2.37%)** |

판정 불가(양측 중 코드 부재)는 지번 1 건, 도로명 22 건이다.
산(山) 구분 일치는 지번 99.95%, 도로명 100.00%.

정규화 규칙(적용 전부): NFC → 공백 축약 → 공백 제거 후 비교 → 시도명 이형 사전 →
**시도코드 12 완화**(§5.7) → 지목 접미 제거 → 산 접두 정규화 → `번지·번·호` 접미 제거 →
지번은 `(san, ji_main, ji_sub)` 3-튜플 비교 → 빈 문자열·`null` 은 **전부 불일치**.

### 5.7 시도코드 12 — 계획의 가정이 반대였다

계획 §4.2 규칙 5 는 **"우리 DB 는 통합 코드(12)를 쓰고 VWorld 는 현행 광주/전남을 쓸 수 있다"**
고 가정했다. **실측 결과는 정반대다.**

| 지점 | VWorld | 우리 8092 |
|---|---|---|
| 전남 순천 인근 | `전남광주통합특별시` / `1215011900` | `전라남도` / `4615011900` |
| 광주 인근 | `전남광주통합특별시` / `1224012000` | `광주광역시` / `2914012000` |
| 충남 예산 (대조군) | `충청남도` / `4481036021` | `충청남도` / `4481036021` |

**통합 코드를 쓰는 쪽은 VWorld 이고, 우리 DB 가 통합 이전 코드(46/29)에 머물러 있다.**
`address` 테이블(`kind='addr'`)의 접두 분포도 `46` 1,159,723 · `29` 163,664 이고 `12` 는 0 이며,
`parcel` 파티션도 `parcel_46`·`parcel_29` 만 있고 `parcel_12` 는 존재하지 않는다.

그래서 오라클 축(원천 PNU ↔ 우리 DB)에서는 **relax12 발동이 0 건**이다 — 원천도 우리 DB 도
구 코드라 일관되기 때문이다(계획 조건 5 의 `BM25[:19]` 채택이 상류에서 문제를 제거했다).
완화가 필요한 곳은 **우리 DB ↔ VWorld 비교축**뿐이고, 거기서는 §5.6·§6.3 처럼 크게 발동한다.

T042/T043 축의 1 급 발견이다. **우리 DB 는 전남광주 통합 행정구역 개편을 아직 반영하지 못했다.**

> **[정정 2026-08-28 · T050]** 표의 "우리 8092" 응답 표기는 **안 A 시도 치환이 꺼진**
> (`_HAS_SIDO_REMAP=False`, §0.0) 런타임의 출력이므로 응답측 관측으로는 오염이다.
> 그러나 **DB 수준 실측 — `address` 접두 46/29 분포, `parcel_12` 부재 — 은 유효**하며,
> "202605 세대 DB 가 통합 개편 미반영"이라는 결론 자체는 선다. 후속: **T045(2026-08-28)가
> 202607 원천으로 전면 재빌드해 geocode.sqlite 측은 해소** — `전남광주통합특별시` 1,323,642 행,
> 인천 신구 4 곳 신코드(구코드 잔존 0). PostGIS 는 T049 재적재 전까지 구세대다. 재적재 후
> 이 축의 relax12 는 양측 모두 신코드가 되어 **불발동이 예상값**이다(§0.0.3-3).

### 5.8 경로(`source`) 별 분해 — §0.1 이 요구한 분리

| 층 | `source` | D0 내 건수 | 비고 |
|---|---|---:|---|
| 지번 | `parcel` | 5,943 (99.95%) | **자기참조 구간.** 좌표 지표는 정확도가 아니다 |
| 지번 | (없음) | 3 | 표본이 작아 지표를 내지 않는다 |
| 도로명 | (없음) | 5,967 (100%) | `parcel` 폴백 없음 |

지번층은 사실상 전량이 `parcel` 경로다. 따라서 **§5.2 의 지번 좌표 분위는 통째로
자기참조 구간의 수치**이며, 그 안에서 유효한 것은 절대 오차가 아니라 §5.5 의 선택 실패다.
`navi` 경로는 지번층 D0 에서 관측되지 않았다.

---

## 6. 역방향 지표 (§4.2)

**먼저 §0.3 을 읽어라.** 이 절의 주소축 수치는 스키마 v2 구간(3,982 건, 20 층, 모집단 28.83%)
에서만 나온다. **전국 값이 아니다.**

### 6.1 결함의 실증 — 무엇이 왜 비었나

`rev_bcode_eq` 가 전건 `None` 인 것을 발견하고 원인을 소표본으로 좁혔다.

| 확인 | 결과 |
|---|---|
| VWorld 역방향 `status` | `OK` |
| VWorld `structure.level4LC` | **10 자리 존재** |
| 우리 `/reverse` 최상위 키 | `address` · `address_source` · `areas` · `contract_version` · `lat` · `lon` · `nearest` — **`results` 없음** |
| 우리 `address.structure.b_code` | **10 자리 존재** |

**VWorld 의 한계가 아니라 우리 추출 코드의 결함이다.** v1 은 `body["results"][0]` 을 읽었는데
그 키가 없어 무조건 `None` 을 돌려줬다 — 순방향 `/geocode` 의 응답 형태를 역방향에 그대로
적용한 오독이다. 같은 오독의 뒷면으로, 계획이 **쓰지 말라고 명시한** `nearest[]` 가
v1 에서 유일하게 채워진 역방향 수치(`rev_dist_m`)였다.

부수 발견: **VWorld 역방향 `structure` 에는 리 명칭 필드가 없다.** `level4L` 은 읍면동,
`level5` 는 지번이고 리 명칭은 `text` 에만 실린다. 계획 §4.2 가 "리 = 코드 10 자리 /
`text` 파싱 리명 **둘 다**"라고 쓴 이유가 이것이다.

### 6.2 호출 결과와 `address_source` (M8)

| 항목 | 지번 (n=1,988) | 도로명 (n=1,994) |
|---|---:|---:|
| VWorld `OK` | 100.00% | 99.95% (`NOT_FOUND` 1) |
| 우리 8092 응답 | 100.00% | 100.00% |
| `pip_key` (필지 PIP — 실주소) | 1,757 (88.38%) | 1,593 (79.89%) |
| `knn` (**최근접 주소점 합성**) | 231 (11.62%) | 401 (20.11%) |

**합성 비율이 지번 11.62% · 도로명 20.11%** 다. 합성 건과 실주소 건을 섞어 정확도를 내면
무엇의 정확도인지 알 수 없으므로 §6.3·§6.4 에 분해를 함께 싣는다.

전 구간(12,000) 기준 역방향 최근접 거리 — **이것은 §4.2 지표가 아니라 `nearest[]` 기반의
별개 보조 관측이다**(계획이 배제한 축이다. §6.1):
지번 p50 5.900 · p95 36.105 · 최대 261.400 m / 도로명 p50 6.600 · p95 44.800 · 최대 871.900 m.

### 6.3 계층 일치율 — 이 수치는 정확도 근거가 아니다

역방향 질의점은 VWorld 순방향이 반환한 좌표이고, 지번 경로에서 양쪽 모두 **같은 연속지적도
필지**로 해석한다. 따라서 계층 코드 일치는 **정확도가 아니라 같은 지적 원천의 재확인**이다.
§0.1 의 한계가 역방향 코드축에도 그대로 적용된다.

> **분모 규약(F5).** 이 절의 분모는 **판정가능**(양쪽 법정동코드가 모두 10 자리로 존재)
> 이다 — 지번 1,988 · 도로명 1,993. §6 서두의 3,982 는 **비게이트 합**(1,988+1,994)이고
> `rev_bcode_eq` 판정가능 합은 3,981 이다. 다음 절(§6.4)은 **비게이트 분모**를 쓴다.
> 두 절의 규약이 다르므로 수치를 옮겨 인용하지 마라.

| 축 | 지번 엄격 | 지번 가중 | 도로명 엄격 | 도로명 가중 |
|---|---:|---:|---:|---:|
| 시도 (앞 2) | 80.03% | 61.72% | 80.03% | 56.50% |
| 시군구 (앞 5) | 80.03% | 61.72% | 80.03% | 56.50% |
| 읍면동 (앞 8) | 80.03% | 61.72% | 80.03% | 56.50% |
| 리 포함 10 자리 | 80.03% | 61.72% | 80.03% | 56.50% |

**완화 전후 병기.**

| 축 | 지번 엄격 → 완화 | 뒤집힌 건 | 도로명 엄격 → 완화 | 뒤집힌 건 |
|---|---|---:|---|---:|
| 시도 | 80.03% → **100.00%** | 397 | 80.03% → **100.00%** | 398 |
| 10 자리 | 80.03% → 86.97% | 138 | 80.03% → 87.26% | 144 |

전 축이 정확히 같은 80.03% 인 것과, **완화 후 시도가 100.00% 가 되는 것**이 §5.7 의 직접 증거다.
불일치 전량이 시도코드 12 ↔ 46/29 쌍이다. 10 자리가 완화 후에도 87% 에 머무는 것은
통합 코드 체계에서 시군구 번호까지 바뀐 건(광주 `2914…` ↔ `1224…`)이 섞여 있기 때문이다.

`address_source` 별로는 지번 `knn` 87.88% · `pip_key` 79.00%, 도로명 `knn` 85.25% ·
`pip_key` 78.72% 다. 합성 경로가 오히려 높은데, 합성은 최근접 **실주소점**을 가져오므로
전남광주 구간의 코드 세대 문제를 덜 타기 때문으로 보인다 — 다만 이 해석은 추가 근거가 없어
**가설로만 남긴다.**

### 6.4 리(里) 4 지표 — 여기서 실제 차이가 드러난다

계층 코드가 전부 일치하는 것과 달리, 리 **문자열** 축에서는 양쪽이 뚜렷이 갈린다.

> **분모 규약(F5).** 이 절의 분모는 **비게이트 건수**(지번 1,988 · 도로명 1,994)이고,
> 상호 불일치율만 **판정가능**(지번 1,988 · 도로명 1,993)을 쓴다. 판정가능 분모로
> 다시 계산하면 도로명 코드상 리 있음은 39.89%, 문자열 채움은 1/1,993 이다.
> §6.3 은 전 지표가 판정가능 분모다.

| 지표 | 지번 우리 | 지번 VWorld | 도로명 우리 | 도로명 VWorld |
|---|---:|---:|---:|---:|
| 코드상 리 있음 (끝 2 자리 ≠ `00`) | 40.04% | 40.04% | 39.87% | 39.87% |
| **리 문자열 채움률** (전체 분모) | **0.00%** (0/1,988) | 39.64% | **0.10%** (2/1,994) | 38.82% |
| **리 문자열 채움률** (코드상 리 있는 건만) | **0.00%** (0/796) | **98.99%** (788/796) | **0.13%** (1/795) | **97.36%** |
| **코드-문자열 상호 불일치율** | **40.04%** | 0.40% | **39.84%** | 1.05% |
| **리 코드 일치율** (끝 2 자리) | 100.00% | — | 100.00% | — |
| **리 문자열 일치율** | **판정 불가** | — | 100.00% (n=1) | — |

**리 문자열 일치율을 0% 로 적지 않는다.** 우리 쪽 채움이 사실상 0 이라 비교 자체가 성립하지
않으므로 "판정 불가(비교 불성립)"가 정확하다. 0% 는 "비교했는데 다 틀렸다"로 읽혀 사실과 다르다.
도로명층의 100%(n=1)도 표본이 1 건이라 지표로 쓸 수 없다.

농촌층만 보면 코드상 리 있음이 지번 100.00% · 도로명 99.87% 인데 문자열 채움은 0.00% · 0.25% 다.

**정정(F6).** A 단계 판은 "`pip_key` 든 `knn` 든 **동일하게 0%**"라고 적었으나 정확히 0 은
아니다. 실측은 `pip_key` **0 / 1,321** · `knn` **1 / 270 = 0.37%** 다. 한 건이라도 채워진
경로가 있다는 사실은 결론을 바꾸지 않는다 — 두 경로 모두 사실상 비어 있고, 따라서
**합성 경로 탓이 아니라 렌더링 자체의 결함**이라는 판정은 그대로다. 다만 "동일하게 0%"는
과장이었다.

**소견.** 우리 역지오코딩은 **법정동코드에는 리를 정확히 담으면서 렌더링된 주소 문자열에서는
리를 통째로 누락**한다. VWorld 는 코드상 리가 있는 건의 99% 에서 리명을 싣는다.

이는 T029 가 원천에서 찾은 결함(`ri` 가 `jibun` 첫 토큰과 어긋나는 행 211,017 건, 일치율
96.94%)과 **방향이 다른 별개 결함**이다. T029 는 "채워졌으나 틀림", 이것은 "코드는 맞는데
문자열이 빔"이다. 둘을 섞어 읽지 마라.

> **[정정 2026-08-28 · T050]** "렌더링 자체의 결함" 판정을 **철회한다.** 채움률 0.00~0.13% 는
> `_HAS_LAWD_RI=False` → `ri_sel`/`ri_join` 빈 문자열 → `ri_nm` 미조회 기전의 산물이다
> (T047 C0-4(a) 재집계·기전 일치, §0.0). 현행 런타임 라이브 재측정(`basis=ours`, 800 건 표본,
> 천장 효과 유의 — T047 검토 5)에서 순방향 top-1 리 채움 99.49%(786/790). 코드에 리 렌더링은
> 구현돼 있었다. ※ "코드상 리 있음 42.58/42.59% 일치"를 오염 판별의 지문으로 쓰지 마라 —
> 리 코드는 사전과 무관하게 항상 정상이다(보조 논거, §0.0). T029 원천 결함(96.94%)과의
> 구분("채워졌으나 틀림" vs "코드는 맞는데 문자열이 빔")은 여전히 유효하되, 후자는 런타임
> 상태의 문제였다.

### 6.5 역방향 도로명 예비조사(§4.2-a) — 예비조사 대신 **본 측정으로 대체**

계획은 이 예비조사를 **다섯 곳**에서 지시했다(M7 · §1.3 · §4.2-a · §8 항목 14 · §11 리스크 3 ·
착수 순서 8). 역방향은 일일 한도가 없어 비용도 사실상 0 이었다.

**A·B 단계에서는 실행되지 않았다.** `measure.py` 가 역방향 URL 에 `type=PARCEL` 을 상수로
박아 두었고 ROAD 경로가 존재하지 않았다. A 단계 보고서 §H 의 미실행 목록에도 이 항목이 없어
B 단계가 코드를 읽고서야 드러났다.

**C 단계에서 `type=both` 로 바꿔 본 측정에 편입했다.** 계획이 지정한 200 건 예비조사의 목적은
"응답률이 0 이 아니면 정식 편입"을 가르는 것이었는데, 역방향에 한도가 없으므로 200 건을
따로 떠볼 이유가 없다 — 12,000 건 전량을 그대로 돌리면 응답률이 곧 본 측정 부산물이 된다.
계획 §8-14 가 요구한 `N/200` 수치는 따라서 **더 큰 분모의 실측치로 대체**한다(§6.7).
`type=both` 는 한 번의 호출로 `result[]` 에 `parcel` 과 `road` 를 함께 싣는다(실측).

### 6.6 T047 수선 명세

| # | 수선 | 위치 | 근거 |
|---|---|---|---|
| 1 | 역방향 추출을 `address.structure` 로 | `measure.py::_reverse_structure` | **B 단계에서 완료(v2)** |
| 2 | 오라클 구 시도코드 보정 42→51 · 45→52 | `oracle.py::sido_legacy_candidates` | B 단계는 **지번·도로 축만** 보정했다. 심판·`O_apx` 축은 C 단계에서 완료(§7.6.1) |
| 3 | **우리 `/reverse` 가 `structure.ri` 를 채우도록 수선** | `geocode-api-pg.py` | §6.4 — 코드엔 이미 있는 정보다 |
| 4 | **순방향 지번 검색에서 리를 식별키로 사용** | `geocode-api-pg.py` | §5.5 · §7.7 — 최대 효과 항목 |
| 5 | 전남광주 통합 시도코드(12) 대응 | 빌드 파이프라인 | §5.7 |
| 6 | 역방향 `type=ROAD` 경로 + 200 건 예비조사 | `measure.py::vworld_reverse_url` | **C 단계에서 완료** — `type=both` 로 바꿔 본 측정에 편입했다(§6.5) |
| 7 | 재호출 일치율 모드 | `measure.py` CLI | §0.4 |
| 8 | `aggregate.py --strata` 기본값 수정 | `aggregate.py::main` | §12.5-5 |
| 9 | `aggregate.py` 의 F6 행은 구조적으로 항상 0 | `aggregate.py` | §7.3 |
| 10 | **오라클 조회 키의 구 시도코드 보정을 축마다 따로 걸지 말 것** | `oracle.py` | §7.6.1 — 축이 늘 때마다 보정을 빠뜨렸다. 키 정규화를 `split_pnu` 진입점 **한 곳**으로 모으고, 새 축은 그 경로를 반드시 통과하게 한다. 축별 계수기는 유지한다(원인 분리) |
| 11 | **PNU 조회를 8 자리 `emd_cd` 로 자르지 말 것** | `oracle.py` · `parcel.emd_cd char(8)` | §7.7 — 리 2 자리가 사라져 이웃 리를 함께 문다. 정확 PNU 19 자리 조인이 더 옳고 더 싸다 |
| 12 | `gate_e1` 계수기가 실제 E1 건수와 어긋난다 | `measure.py::Counters.gate_e1` | §12.5-10 |

### 6.7 역방향 주소축 재측정 (C 단계) — **60 층 전체**

`type=both` 로 역방향만 다시 돌렸다. **VWorld 순방향은 한 건도 부르지 않았다** —
역방향은 호출 한도가 없다(계획 §1.1). 질의 좌표 기준의 성질은 §0.3.1 에 있다.

#### 6.7.1 기준 좌표 확보 — 덮지 못한 구간을 먼저 적는다

| 기준 | 표본 | 좌표 확보 | 층 | 결손 사유 |
| --- | ---: | ---: | ---: | --- |
| `src` | 12,000 | 11,043 (92.03%) | 60 / 60 | 필지 대표점 부재(우리 DB 에 그 PNU 가 없다) **936** · 원천 좌표 없음(출입구·건물중심 모두 부재) **21** |
| `fwd` | 12,000 | 11,986 (99.88%) | 60 / 60 | 우리 순방향이 주소 후보를 내지 않음 **14** |

**`src` 지번의 결손 936 건은 실제 데이터 부재다.** 파티션 무관 `count(*)` 로 직접 확인했다 —
우리 `parcel` 에 그 PNU 가 없다. 이것이 `fwd` 기준을 함께 돌린 이유이기도 하다.

**F2 보정은 이 새 축에서도 실제로 작동했다.** `legacy_reprpt_attempts 787 / hits 707` —
구 시도코드 보정이 없었다면 결손이 957 → **1,664 건(13.87%)** 이 됐을 것이다. 축을 새로
만들 때마다 보정을 빠뜨리는 것이 §7.6.1 이 지적한 결함이므로, 이 축은 처음부터 같은
보정 경로를 통과하게 만들었다.

#### 6.7.2 호출 결과 — **VWorld 역방향은 도로명을 준다**

계획 §8-14 가 요구한 "역방향 도로명 응답률"이다. 200 건 예비조사 대신 본 측정의
실측치로 답한다. **응답률은 0 이 아니다.**

| 기준 · 층 | n | `status=OK` | 지번 항목 | **도로명 항목** | 우리 8092 응답 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `src` · jibun | 5,064 | 100.00% | 100.00% | **64.57%** | 100.00% |
| `src` · road | 5,979 | 99.97% | 99.93% | **56.92%** | 100.00% |
| `fwd` · jibun | 5,991 | 100.00% | 100.00% | **56.42%** | 99.82% |
| `fwd` · road | 5,995 | 99.97% | 99.93% | **56.86%** | 99.98% |

**A 단계의 가정이 틀렸다.** §6.5 는 "VWorld 역방향이 도로명을 주지 않을 수도 있다"를
전제로 200 건 예비조사를 설계했는데, 실측은 **절반 이상의 지점에서 도로명이 온다.**
도로명 축이 비어 있던 것은 VWorld 의 응답 한계가 아니라 우리가 `type=PARCEL` 로
고정해 **요청하지 않았기** 때문이다.

#### 6.7.3 축별 일치율 — 판정가능 분모, 정규화 후

| 축 | `src` 지번 | `src` 도로명 | `fwd` 지번 | `fwd` 도로명 |
| --- | ---: | ---: | ---: | ---: |
| 시도 (앞 2) | 99.98% | 100.00% | 99.98% | 100.00% |
| 시군구 (앞 5) | 93.86% | 94.56% | 94.57% | 94.54% |
| 읍면동 (앞 8) | 93.86% | 94.54% | 94.57% | 94.52% |
| **리 포함 10 자리** | 93.82% | 94.53% | 94.53% | 94.51% |
| 지번 **본번** | 99.90% | 97.24% | 99.63% | 97.21% |
| 지번 **부번** | 99.90% | 96.10% | 99.46% | 96.09% |
| 산 여부 | 100.00% | 99.97% | 100.00% | 99.97% |
| **도로명** | 98.84% | 98.35% | 98.61% | 98.47% |
| **건물 본번** | 97.16% | 96.00% | 96.83% | 96.19% |
| **건물 부번** | 97.46% | 96.50% | 97.45% | 96.60% |

판정가능 건수(같은 순서):

| 축 | `src` 지번 | `src` 도로명 | `fwd` 지번 | `fwd` 도로명 |
| --- | ---: | ---: | ---: | ---: |
| 리 포함 10 자리 | 5,064 | 5,975 | 5,980 | 5,990 |
| 지번 본번 | 5,064 | 5,975 | 5,980 | 5,990 |
| 도로명 | 3,270 | 3,403 | 3,374 | 3,408 |
| 건물 본번 | 3,270 | 3,403 | 3,374 | 3,408 |

**읽는 법.** 지번 축의 `src` 지번층 수치(99.90%)는 **자기참조 구간**이다 — 점을 뽑은 그
필지를 우리 `pip_key` 가 다시 문다. 상한으로만 읽어라. 나머지 세 칸(`src` 도로명층,
`fwd` 두 층)에는 그 항진명제가 없다.

**법정동코드 10 자리가 약 94% 에 머무는 것이 이 표의 핵심이다.** 시도는 완화 후 거의
100% 인데 시군구부터 6%p 가 떨어진다 — §5.7 이 짚은 전남광주 통합(광주 `2914…` ↔
`1224…`)처럼 시군구 번호까지 바뀐 구간이다. 리(끝 2 자리) 자체는 거의 100% 일치하므로,
**코드 축의 불일치는 리가 아니라 상위 행정구역 코드 세대 문제다.** 리 결함은 코드가
아니라 **문자열** 축에서 나타난다(§6.7.5).

**완화 전(`_strict`) 병기 — §5.6 과 대칭.** 위 표의 계층 4 축은 §4.3 조건 1 의
**시도코드 12 완화**를 건 뒤의 값이다. 완화 전 값을 함께 낸다 — 순방향 §5.6 이
법정동코드에 대해 하는 것과 같은 병기다. `_strict` 는 **"정규화 전"이 아니라 "완화 전"**
이다: 계층 축은 숫자 코드라 문자열 정규화의 여지가 없고, `revmetrics._prefix_strict` 와
`_prefix_norm` 의 유일한 차이가 `relax12` 다. **§6.7.4 의 정규화 3 단과 다른 축이다.**
지번·도로명·건물 축은 이 완화의 대상이 아니므로 이 표에 없다.

| 축 | `src` 지번 | `src` 도로명 | `fwd` 지번 | `fwd` 도로명 |
| --- | ---: | ---: | ---: | ---: |
| 시도 (앞 2) | 92.30% → 99.98% | 93.34% → 100.00% | 93.29% → 99.98% | 93.32% → 100.00% |
| 시군구 (앞 5) | 91.15% → 93.86% | 92.15% → 94.56% | 92.26% → 94.57% | 92.12% → 94.54% |
| 읍면동 (앞 8) | 91.15% → 93.86% | 92.13% → 94.54% | 92.26% → 94.57% | 92.10% → 94.52% |
| 리 포함 10 자리 | 91.11% → 93.82% | 92.12% → 94.53% | 92.22% → 94.53% | 92.09% → 94.51% |

완화가 뒤집은 건수(판정가능 분모 — 위 표와 같은 분모):

| 축 | `src` 지번 | `src` 도로명 | `fwd` 지번 | `fwd` 도로명 |
| --- | ---: | ---: | ---: | ---: |
| 시도 (앞 2) | 389 / 5,064 | 398 / 5,975 | 400 / 5,980 | 400 / 5,990 |
| 시군구 (앞 5) | 137 / 5,064 | 144 / 5,975 | 138 / 5,980 | 145 / 5,990 |
| 읍면동 (앞 8) | 137 / 5,064 | 144 / 5,975 | 138 / 5,980 | 145 / 5,990 |
| 리 포함 10 자리 | 137 / 5,064 | 144 / 5,975 | 138 / 5,980 | 145 / 5,990 |

**읽는 법.** 완화가 움직이는 폭은 시도 축에서 **6.66~7.68 %p** 로 가장 크고, 시군구
이하에서는 **2.31~2.71 %p** 다. 바로 위 문단의 "시도는 완화 후 거의 100%"는 그 **완화 후**
값이고, 완화 전에는 92.30~93.34% 다. 두 표를 겹쳐 보면 시도 축에서 완화로 살아난
389~400 건 중 **252~262 건이 시군구 이하에서 다시 어긋난다**(칸별 실측 `src` 지번 252 · `src` 도로명 254 · `fwd` 지번 262 · `fwd` 도로명 255. 예로 `src` 지번은 389 중 252 가 어긋나고 137 만 시군구까지 유지된다).
§5.7 이 짚은 대로 통합이 시도 두 자리만이 아니라 **시군구 번호까지** 바꾼 구간이 있다는
뜻이며, 이 표가 그 사실을 건수로 뒷받침한다.

자기검증: 완화가 일치를 **깬** 건 전 칸 **0**, 완화 전후 판정가능 분모 차 전 칸 **0**.
완화는 조건을 넓히기만 했다(집계 코드가 이 두 값을 항상 함께 내고, 0 이 아니면 표에
"자기검증 실패"로 드러낸다).

재현(측정 아님 — 원자료 재집계다. VWorld 를 부르지 않는다):

```bash
/usr/bin/python3 scripts/t046/strict_table.py --tags src fwd
# → ~/geocode-build/t046/out/report/rev2_strict.{md,json}
```


#### 6.7.4 정규화 전후 — 기여분을 수치로

`원문` 은 문자열 그대로, `bare` 는 공백만 제거하고 `\d+(-\d+)?` 만 인정, 
`정규화` 는 §4.2 규칙 1~12 전체다. 아래는 `src` 기준 두 층 합산이다.

| 축 | 원문 일치 | bare 판정가능 | bare 일치 | 정규화 판정가능 | 정규화 일치 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 지번 본번 | 72.60% | 8,140 | 99.14% | 11,039 | 98.46% |
| 지번 부번 | 72.60% | 8,140 | 98.75% | 11,039 | 97.84% |
| 도로명 | 98.59% | — | — | 6,673 | 98.59% |
| 건물 본번 | 95.46% | 6,673 | 96.57% | 6,673 | 96.57% |
| 건물 부번 | 95.46% | 6,673 | 96.97% | 6,673 | 96.97% |

`bare` 단은 지목 접미(`"638-1도"`)·산·번지를 읽지 못한다. 그래서 `bare` 가 판정하지 못한
**2,899 건(11,039 중)** 이 곧 정규화의 기여분이다. 원문 그대로 비교하면 지번 일치율이
72.60% 인데 정규화하면 98.46% 가 된다 — **25.9 %p 가 표기 이형이지 실제 불일치가 아니다.**
도로명·건물번호는 정규화 전후 차이가 1 %p 안팎으로 작다. 이형이 거의 없다.

적용 규칙은 §4.2 규칙 1~12(`scripts/t046/normalize.py`)다. 이 표에서 실제로 작동한 것은
**공백 제거 · 지목 접미 제거 · `번지`/`번`/`호` 접미 제거 · 산 접두 분리 · 부번 부재를
0 으로 정규화** 다섯 가지다.

#### 6.7.5 리(里) 4 지표 — 60 층

| 기준 · 층 | n | 리 문자열 채움(우리) | 리 문자열 채움(VWorld) | 리 코드 일치 | 리 문자열 일치 (판정가능) | 코드-문자열 상호 불일치(우리) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `src` · jibun | 5,064 | **0.00%** (0) | 42.69% | 99.98% | — (0) | **43.27%** |
| `src` · road | 5,979 | **0.27%** (16) | 44.94% | 100.00% | 91.67% (12) | **46.36%** |
| `fwd` · jibun | 5,991 | **0.00%** (0) | 45.99% | 99.98% | — (0) | **46.51%** |
| `fwd` · road | 5,995 | **0.27%** (16) | 45.04% | 100.00% | 91.67% (12) | **46.44%** |

**우리는 법정동코드에 리를 담으면서 주소 문자열에서는 리를 내지 않는다.** 채움률
0.00%~0.27% 대 VWorld 42.69%~45.99% 이고, 코드-문자열 상호 불일치가 **43~47%** 다.
네 칸이 모두 같은 방향을 가리키므로 좌표 기준의 문제가 아니다.

**도로명층에서 문자열 일치율이 처음으로 판정가능해졌다** — 12 건에서 91.67%. 표본이
작아 지표로 쓸 수는 없지만, B 단계의 "판정 불가"에서는 벗어났다. 우리가 리를 **낼 때는**
대체로 맞다는 뜻이며, 결함이 정확도가 아니라 **렌더링 누락**이라는 §6.4 의 판정을 지지한다.

> **[정정 2026-08-28 · T050]** 이 표의 "리 문자열 채움(우리)" 열은 §6.4 와 같은 오염
> (`ri_nm` 미조회)의 산물이다 — 60 층 재측정도 같은 오염 인스턴스(2026-08-21T04:45:26Z 기동)를
> 상대로 수행됐다. "렌더링 누락" 판정은 철회한다(§0.0.1). 리 **코드** 축(99.98~100.00%)과
> VWorld 측 수치는 유효하다.

#### 6.7.6 `address_source` 분해 (계획 §8-15) — 60 층

| 기준 · 층 | `address_source` | 건수 | 비율(합성 비율) | 10 자리 | 지번 본번 | 도로명 | 건물 본번 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `src` · jibun | `knn` | 56 | 1.11% | 91.07% | 96.43% | 78.95% | 63.16% |
| `src` · jibun | `pip_key` | 5,008 | 98.89% | 93.85% | 99.94% | 98.95% | 97.35% |
| `src` · road | `knn` | 1,589 | 26.58% | 95.39% | 93.82% | 99.55% | 99.25% |
| `src` · road | `pip_key` | 4,390 | 73.42% | 94.21% | 98.47% | 98.06% | 95.22% |
| `fwd` · jibun | `knn` | 1,686 | 28.14% | 94.90% | 98.87% | 95.16% | 90.97% |
| `fwd` · jibun | `pip_key` | 4,294 | 71.67% | 94.39% | 99.93% | 98.96% | 97.42% |
| `fwd` · jibun | `None` | 11 | 0.18% | — | — | — | — |
| `fwd` · road | `knn` | 1,586 | 26.46% | 95.32% | 93.81% | 100.00% | 100.00% |
| `fwd` · road | `pip_key` | 4,408 | 73.53% | 94.22% | 98.43% | 98.11% | 95.27% |
| `fwd` · road | `None` | 1 | 0.02% | — | — | — | — |

#### 6.7.7 두 기준의 대조 — **기준 선택이 지표를 움직이지 않는다**

질의 좌표 기준이 원본과 다르다는 것이 이 재측정의 최대 약점이다. 그 약점의 크기를
잰다. **양 기준이 모두 좌표를 만든 11,036 건**만 골라 같은 자리에서 비교했다.

| 축 | `src` | `fwd` | 차 |
|---|---:|---:|---:|
| 시도 | 99.99% | 99.99% | 0.00 %p |
| 시군구 | 94.23% | 94.23% | 0.00 %p |
| 읍면동 | 94.23% | 94.22% | 0.00 %p |
| 리 포함 10 자리 | 94.20% | 94.19% | 0.00 %p |
| 지번 본번 | 98.46% | 98.39% | 0.06 %p |
| 지번 부번 | 97.84% | 97.71% | **0.14 %p** |
| 산 여부 | 99.98% | 99.98% | 0.00 %p |
| 도로명 | 98.61% | 98.58% | 0.03 %p |
| 건물 본번 | 96.57% | 96.54% | 0.03 %p |
| 건물 부번 | 96.97% | 96.98% | 0.01 %p |

**전 축에서 차이가 0.14 %p 이하다.** 자기참조가 있는 지번 축조차 0.06~0.14 %p 밖에
벌어지지 않는다.

**그러나 이것을 "원본 기준으로도 같았을 것"의 근거로 쓰지 않는다.** 잰 것은 *두 대체
기준 사이의 차이*이지 *대체 기준과 원본 기준의 차이*가 아니다. 원본 기준(VWorld 순방향
좌표)은 재현할 수 없으므로 그 비교는 영원히 불가능하다. 여기서 말할 수 있는 것은
**"기준을 바꿔도 이 정도밖에 안 움직인다"** 까지다.

#### 6.7.8 호출 집계와 관측된 이상

| 항목 | `src` 창 | `fwd` 창 |
|---|---|---|
| **VWorld 순방향 호출** | **0** | **0** |
| VWorld 역방향 호출 | 11,051 | 12,003 |
| VWorld `status` | `OK` 11,041 · `NOT_FOUND` 2 | `OK` 11,984 · `NOT_FOUND` 2 |
| VWorld 재시도 / 포기 | 8 / **0** | 17 / **0** |
| VWorld p95 지연 | 0.906 s | 0.861 s |
| 우리 8092 호출 | 역 11,100 | 순 12,003 · 역 12,134 |
| 우리 `status` | 역 `OK` 11,043 | 순 `OK` 12,000 · 역 `OK` 11,974 · **`HTTP_503` 12** |
| 우리 재시도 / 포기 | 46 / **0** | 104 / **12** |
| 우리 p95 지연 | 역 1.645 s | 순 0.110 s · 역 2.153 s |
| `OVER_REQUEST_LIMIT` · HTTP 429 | **0** | **0** |
| 게이트 E1 / E2 | 2 / 0 | **0** / **12** |

**순방향 예산 소모 0.** 역방향에는 호출 한도가 없다(§1.1). 그럼에도 8 req/s 토큰버킷과
백오프(1·2·4 s, 최대 3 회)는 그대로 걸었다.

**관측된 이상 1 — 우리 `/reverse` 가 부하에서 503 을 낸다.** `fwd` 창에서 12 건이
`HTTP_503 PostGIS 연결 실패: canceling statement due to statement timeout` 으로 끝났다
(12,134 호출 중 **0.10%**). `fwd` 창은 순방향 호출이 더해져 우리 서버 부하가 `src` 창의
약 2 배였고, `src` 창의 실패는 **0 건**이었다. 이 12 건은 게이트 E2 로 계상돼 **축이 전부
판정불가로 남는다** — 부재로 세지 않았다.

**관측된 이상 2 — `/health` 가 측정 중 내내 503 이었다.** `fwd` 창의 시작·종료 프로브가
모두 `ERR:HTTP Error 503` 이다. 같은 시각 `/geocode`·`/reverse` 를 직접 호출하면 HTTP 200
에 완전한 응답이 왔고, 측정 종료 직후 `/health` 는 **0.71 s 에 200**(`places 16,282,127`,
창 시작값과 동일)으로 회복했다. `/health` 가 `COUNT(*)` 를 돌기 때문으로 보이며,
§2 가 기록한 재기동 직후 현상과 같은 계열이다. `window_same` 비교 항목(컨테이너 ID ·
마운트 · md5 × 2 · pg ID)은 **양 창 모두 True** 다.

**관측된 이상 3 — `src` 창의 `gate_e1` 이 2 로 찍혔다.** 이 창에는 순방향 호출이 0 건이라
E1 은 0 이어야 한다. §12.5-10 이 지적한 계수기 결함(방향 미구분)이 그대로 재현된 것이며,
역방향 `NOT_FOUND` 2 건을 E1 로 센 결과다. 수선을 창이 돌기 시작한 뒤에 넣어 `src`
프로세스에는 반영되지 않았고, **`fwd` 창에서는 `gate_e1 = 0`** 으로 정정됐다. 이 계수기는
집계에 쓰이지 않으므로 지표에는 영향이 없다.

### 6.8 완료조건 2 재판정 — "§4.2 역방향 지표 전체"

**뭉개지 않고 항목별로 판정한다.** "부분 충족"이라는 말은 쓰지 않는다.

| §4.2 지표군 | A·B 단계 | C 단계 | 판정 |
|---|---|---|---|
| 시도 일치율 | 20/60 층 | **60/60 층** | **충족** |
| 시군구 일치율 | 20/60 층 | **60/60 층** | **충족** |
| 읍면동 일치율 | 20/60 층 | **60/60 층** | **충족** |
| 리 4 지표(채움·코드·문자열·상호불일치) | 20/60 층 | **60/60 층** | **충족** |
| 지번 본번·부번 일치율 (+산 여부) | **미산출** | **60/60 층** | **충족** |
| 도로명 일치율 | **미산출** | **60/60 층** | **충족** |
| 건물번호 본번·부번 일치율 | **미산출** | **60/60 층** | **충족** |
| 정규화 전후 병기 | 시도코드 완화만 | **축마다 3 단** | **충족** |
| `address_source` 별 분리 + 합성 비율(§8-15) | 20/60 층 | **60/60 층** | **충족** |

**지표군 자체는 전부 산출됐다. 그러나 완료조건 2 를 "충족"으로 선언하지 않는다.**
§4.2 는 지표 목록만이 아니라 **질의 좌표 규약**도 정했기 때문이다 —
*"질의 좌표 = VWorld 순방향 반환 좌표, 양쪽에 같은 좌표."*

| 규약 | 상태 |
|---|---|
| 양쪽(우리·VWorld)에 **같은 좌표**를 준다 | **지킴** |
| 그 좌표가 **VWorld 순방향 반환 좌표**여야 한다 | **어김.** 그 좌표는 저장되지 않았고 순방향 재호출은 금지다 |

**따라서 판정은 이렇다 — 지표 목록은 전량 충족, 좌표 규약은 미충족이며 복구 불가능하다.**
복구하려면 순방향 12,000 건을 다시 불러야 하고(예산 30%), 그것은 태스크가 명시적으로
금지했다. 남은 선택지는 "지표를 내지 않는다"와 "다른 좌표 기준으로 낸다"뿐이었고
후자를 택했다. 그 기준의 성질과 자기참조 구간은 §0.3.1 과 §6.7 에 적었다.

**이전 판정과의 관계.** B 단계 보고서는 §0.3 말미에서 "완료조건 2 는 **부분 충족**"이라고
적었다. 그것은 **A·B 단계 기준으로는 정확한 판정**이었다 — 지표 3 군이 아예 없었고 나머지도
20/60 층뿐이었다. C 단계가 바꾼 것은 지표 산출 여부이지 좌표 규약이 아니다.

---

## 7. 실패 원인 분류 (§4.3)

배타 11 종은 **첫 일치 우선**으로 하나만 붙는다. 분모는 D0 다.

**도달가능성.** 11 개 분류 전부에 대해 도달 입력이 존재함이 단위 테스트로 증명되어 있다
(`tests/t046/test_classify.py::TestReachability`). 따라서 **0 건 보고는 "설계상 도달 불가"가
아니라 "이 표본에서 관측되지 않음"**을 뜻한다.

### 7.1 배타 분류표

| 분류 | 뜻 | 지번 (D0 5,946) | 도로명 (D0 5,967) |
|---|---|---:|---:|
| 1 | 파서·조회 실패 (우리 DB 에 있는데 못 찾음) | 1 (0.02%) | 5 (0.08%) |
| 2 | 적재 커버리지 결손 | 0 | 0 |
| 3 | 근사매칭 부재 | 0 | 0 |
| 4 | 양측 부재 | 0 | 0 |
| **5** | **임계 내 정상** | **4,543 (76.40%)** | **5,732 (96.06%)** |
| **6** | **후보 순위 오류** | **1,186 (19.95%)** | 1 (0.02%) |
| 7 | VWorld 측 의심 | **0** | **0** |
| 8 | 우리 좌표 이상 | **0** | **0** |
| 9 | 좌표 정의 차이 (둘 다 필지 안) | 123 (2.07%) | 0 |
| 10 | 양측 이탈 | 21 (0.35%) | 0 |
| 11 | 심판 자료 부재 | 72 (1.21%) | 229 (3.84%) |

**분류 6 이 지번층의 19.95%** 다. §5.5 가 규명한 리 식별 실패가 여기에 계상된다.

### 7.2 분류 8 의 두 분모

분류 8(우리 좌표 이상)은 지번층 `source='parcel'` 구간에서 구조적으로 나올 수 없다 —
필지 대표점은 정의상 그 필지 안이라 심판 R(M) 이 강제로 참이 된다.

| 층 | 유효 분모 (해당 구간 제외) | 유효 분모 기준 | 전체 분모 기준 |
|---|---:|---:|---:|
| 지번 | 0 | 산출 불가 | 0.00% (0/216) |
| 도로명 | 229 | 0.00% | 0.00% (0/229) |

지번층의 유효 분모가 **0** 이라는 사실 자체가 §0.1 자기참조의 정량적 표현이다.

### 7.3 직교 플래그 (분모 D0)

| 플래그 | 뜻 | 지번 | 도로명 |
|---|---|---:|---:|
| F1 | 법정동코드 불일치 | 29.36% | 9.22% |
| F2 | 카테고리 오폴백 (`kind≠addr` 만) | 0.00% | 0.05% |
| F3 | 순위 오염 (임계 내 후보가 밀림) | **19.95%** | 0.02% |
| F4 | 산번지 구분 어긋남 | 0.05% | 0.00% |
| F5 | 폴백 경로 응답 (`navi`/`parcel`) | 99.95% | 0.00% |
| F6 | 시도코드 12 완화가 판정을 뒤집음 | — (아래) | — |
| F7 | 정규화가 판정을 살림 | 18.82% | 0.00% |
| F8 | A 군 밖 `parcel` 폴백 ∧ O=P | 0.98% | 0.00% |

**F6 행에 대한 주의.** `aggregate.py` 는 **엄격** 플래그 집합에서 F6 을 세는데, `_f6` 은
`relax12=False` 이면 항상 `False` 를 돌려주므로 이 행은 **구조적으로 항상 0** 이다.
실제 완화 뒤집힘은 §5.6 의 지번 107 건 · 도로명 141 건이다. 이 표의 F6 을 "완화가 필요 없었다"로
읽으면 안 된다. `aggregate.py` 의 보고 결함이며 T047 수선 목록(§6.6-9)에 넣었다.

### 7.4 분류 7·8 육안 대조 — 대상이 0 건이다

계획 §4.4-4 는 분류 7(VWorld 측 의심)·8(우리 좌표 이상) 각각 최대 20 건의 육안 대조를
요구한다. **두 분류 모두 관측 건수가 0 이므로 대조할 대상이 없다.**
지지/기각/판단불가 건수는 전부 0 이다.

도달가능성 테스트를 통과했으므로 이 0 은 "설계상 도달 불가"가 아니라 "이 표본에서 미관측"이다.
그리고 §0.2 의 원칙상, 분류 7 을 낼 근거(원천 대조·지도 육안)를 이번 측정에서 수집하지 않았으므로
**설령 후보가 있었더라도 "판정 불가"로 남겼을 것이다.**

### 7.5 심판 부재(분류 11)

지번 1.21% · 도로명 3.84% 로, 계획이 정한 경보 임계 **15% 를 크게 밑돈다.**
심판이 판정한 건은 지번에서 `r_v`·`r_m` 각각 `True 123 / False 21 / None 5,802` 이고,
도로명은 전건 `None` 이다(도로명 심판은 출입구·건물중심 앵커가 필요한데 해당 자료가
판정 시점에 확보되지 않았다).

**그러나 이 1.21% 는 심판이 판정할 수 없었던 건이 아니라, 심판이 잘못된 키로 조회해
자료를 못 찾은 건을 상당 부분 포함한다.** 두 결함이 겹쳐 있다.

| 사실 | 값 |
|---|---:|
| 지번 분류 11 | 72 |
| 그중 PNU 접두가 구 시도코드(42·45) | **22 (30.6%)** |
| 지번 심판이 실제로 판정한 건 | 144 |
| 그중 강원·전북 | **0** |

즉 강원·전북은 **한 건도 심판을 받지 못했다.** 심판 축에 구 시도코드 보정이 없었기
때문이다(§7.6.1). 편향 방향은 **보수적** — 판정을 받았다면 우리가 맞았을 수도 틀렸을 수도
있는데, 자료 부재로 처리되어 우리 결함이 분류표에 드러나지 않았다.

나머지 50 건과 심판이 판정한 144 건에는 **리 절단 결함**(§7.7)이 걸려 있다. 두 수선을
모두 적용하면 지번 분류 11 은 72 → **61**, 심판 판정은 `참 0 / 거짓 155` 가 된다.

### 7.6 오라클 결함 — 구 시도코드 42·45 (조건 1-b, B 단계 발견)

**결함.** 원천 202607 은 강원을 `42`, 전북을 `45`(구 코드)로 싣는데 우리 DB 는 `51`·`52`
(현행)로 적재돼 있다. `parcel_42`·`parcel_45` 파티션은 **존재하지 않는다**.
그 결과 해당 건이 전부 `O=N`("우리 DB 에 없다")으로 오판됐다.

| 실측 | 값 |
|---|---|
| 시도별 `O=N` 비율 | gangwon 774/800 (96.75%) · jeonbuk 795/800 (99.38%) · 그 외 0~39.75% |
| 표본 PNU 접두 | gangwon `42` 774 / `51` 26, jeonbuk `45` 795 / `52` 5 — **`O=N` 건수와 1:1 일치** |
| `parcel` 적재 | `parcel_51` 2,757,489 · `parcel_52` 3,887,040 · `parcel_42`·`parcel_45` **없음** |
| 영향 건수 | 1,560 건 (표본의 13.00%) |
| **접두 치환 후 회수율** | **1,546/1,560 = 99.10%** (강원 99.10%, 전북 99.10%) |
| 미회수 | 14 건 (0.90%) — 강원 9 · 전북 5 |

**그러나 이 결함이 분류표를 바꾸지는 않았다.** 사후 재계산 결과 **분류 이동 0 건**이다.

이유는 오라클이 **A 군(무응답)에서만** 분류를 가르기 때문이다. A 군은 `our_addr_count == 0`
을 요구하는데 전 표본에서 그런 건이 **6 건**뿐이다. 문제의 1,560 건은 전부 응답이 있어
분류 5(1,365) · 6(151) · 11(44) 에 있었고, **분류 3·4 는 0 건**이었다.
즉 "커버리지 결손으로 면책된다"는 경로는 이 표본에서 **발생하지 않았다.**

바뀌는 것은 **오라클 분포 자체**다. 보정 전 지번 `{A 4,522, N 1,366, P 58}` ·
도로명 `{A25 4,467, A19 1, N 1,438, P 61}` 에서 `N` 의 상당수가 실제로는 우리 DB 에 있다.
"우리 DB 에 없는 주소가 N%" 형태의 진술을 이 수치로 하면 틀린다. F8 은 5 건 신규 발동한다.

**완화 전/후 병기.** 보정 전 `O=N` 2,804 건 → 보정 후 1,258 건. 분류 분포는 불변.
완화 계수기는 `relax12`(시도코드 12, 0 건 발동)와 **분리해** `legacy_attempts 1,560 /
legacy_hits 1,546` 으로 집계한다. 원인도 수선 주체도 다르기 때문이다.

**미회수 14 건의 원인.** 7 건은 `pnu_mismatch = True`(조립 PNU 와 `BM25[:19]` 가 갈린 건)로
설명된다. 나머지 7 건은 **원인 미상**이다. 전량 회수를 전제하지 않는다.

**빌드 파이프라인의 비일관성 자체가 발견이다.** 강원·전북은 재매핑했고(42→51, 45→52),
전남광주는 하지 않았다(46/29 유지). 반면 VWorld 는 전남광주를 통합 코드 12 로 이미 옮겼다(§5.7).
우리 빌드가 행정구역 개편을 **선별적으로만** 따라가고 있다.

#### 7.6.1 정정 — B 단계의 "완료"는 **두 축뿐**이었다

앞 절과 §6.6-2 · §12.5-3 은 이 보정을 "B 단계에서 완료"로 적었다. **그 표기는 틀렸다.**
B 단계가 보정한 것은 **지번 축(`jibun_batch`)과 도로 축(`road_batch`)뿐**이고,
심판 축(`referee_parcel_batch`)과 본번 근사 축(`apx_batch`)에는 보정이 없었다.
같은 파일 안의 두 축이 서로 다른 코드 세대를 보고 있었고, 보고서는 그것을 완료라고
선언했다. 숨겨진 한계이자 거짓 주장이다 — 이 프로젝트가 반복해 당한 사고 유형이다.

| 축 | B 단계(HEAD `8f87b9c`, 구 `9e26fdd`) | C 단계 | 편향 방향 |
|---|---|---|---|
| 지번 `jibun_batch` | 보정 있음 | 유지 | — |
| 도로 `road_batch` (BM25 접두 포함) | 보정 있음 | 유지 | — |
| 본번 근사 `apx_batch` | **없음** | `_apply_legacy_apx` 추가 | 근사조차 미적중 → **우리에게 불리** |
| 심판 `referee_parcel_batch` | **없음** | `_apply_legacy_referee` 추가 | 자료 부재(분류 11) → 판정 회피 |
| 대표점 `repr_point_batch`(C 단계 신설) | — | `_apply_legacy_reprpt` | 역방향 기준 좌표 결손 |

계수기도 축마다 분리했다(`legacy_apx_*` · `legacy_referee_*` · `legacy_reprpt_*`).
§7.6 이 이미 발표한 `legacy_attempts 1,560 / legacy_hits 1,546` 은 지번·도로 축만의
수치이므로, 새 축의 시도·적중을 여기에 합산하면 **이미 발표한 숫자의 의미가 사후에 바뀐다.**

**재분류 실측(F2 축 단독, 리 수정 없음).** 지번 D0 = 5,946.

| 지표 | 보정 전 | 보정 후 | 차 |
|---|---:|---:|---:|
| `O_apx` 참 | 4,438 | 5,159 | **+721** (시도 781, 적중 721) |
| 심판 자료 부재(`r_m = None`) | 72 | 3 | **−69** … 그중 구 시도코드 기인 22 건: 시도 22 · 적중 19 |
| 분류 11 중 구 시도코드 기인 | 22 / 72 (30.6%) | — | 검수 실측과 일치 |

**분류 11 의 그 22 건은 어디로 갔나.** F2 보정만 걸면 19 건이 심판 판정을 받고
(참 17 · 거짓 2), 3 건은 여전히 자료 부재다. **그러나 리 절단 결함(§7.7)을 함께 고치면
그 17 건의 '참'은 하나도 남지 않는다** — 참을 만들던 것이 바로 절단 조인이었다.
두 수선을 모두 적용한 상태에서 심판 판정은 `참 0 / 거짓 155 / 부재 61` 이다.

이 방향은 전부 **우리에게 불리하다.** 보정하지 않은 쪽이 우리에게 유리했다는 뜻이며,
그래서 이 정정은 우리 점수를 올리지 않는다.

### 7.7 심판·근사 축의 **리(里) 절단** — C 단계 발견

**결함.** `split_pnu` 가 조회 키를 `emd_cd = pnu[:8]` 로 잘랐고 `parcel.emd_cd` 도
`character(8)` 이라, 법정동코드 10 자리에서 **리 2 자리가 사라진 채** 조인됐다.
그 결과 `bool_or(ST_Contains(...))` 는 심판이 답해야 할 질문이 아니라
**"같은 읍면동 안 *아무* 동일번지 필지에 점이 들어가는가"** 를 답하고 있었다.
존재 판정(`O=P`)과 본번 근사(`O_apx`)도 같은 키를 썼다.

**수선.** `parcel_sido_cd_pnu_key (sido_cd, pnu)` 가 UNIQUE 이므로 정확 PNU 19 자리로
조인한다 — **더 옳으면서 더 싸다**(대조 실행에서 오라클 단계가 약 6.4 배 빨랐다).
근사 축은 부번만 버려야 하므로 색인 경로를 유지한 채 `substr(p.pnu, 1, 10)` 로 리를 되건다.

**PIP 실증.** 절단 조인에서 `r_m = True` 였던 **123 건 전부**를 정확 PNU 로 다시 물었다.

| 관측 | 값 |
|---|---:|
| 뒤집힌 판정 | 123 / 123 |
| 그 점을 포함하는 필지를 실제로 찾음 | 123 (찾지 못한 건 0) |
| 점이 들어 있던 필지 수 | 전건 **정확히 1 개** |
| 그 필지의 정체 | **전건 "다른 법정동(리)의 동일 번지"** |

즉 우리 순방향은 그 123 건에서 읍면동까지는 맞히고 **리를 틀린다.** §5.5 가 순방향에서
관측한 결함과 같은 것이 심판 축에서도 관측된 셈이다.

**분류 영향 — 네 칸을 분리해 재계산했다**(축을 섞으면 어느 수선의 효과인지 알 수 없다).

| 상태 | 오라클(지번) | `O_apx` 참 | 심판 `r_m` (참/거짓/부재) | 분류 9 |
|---|---|---:|---|---:|
| S0 보고서 게재값 재현 | A 4,522 · N 1,366 · P 58 | 4,438 | 123 / 21 / 72 | 123 |
| S1 리 수정만 | A 4,522 · N 1,375 · P 49 | 4,415 | **0 / 137 / 79** | **0** |
| S2 리 수정 + F2 | A 5,291 · N 601 · P 54 | 5,134 | **0 / 155 / 61** | **0** |

S0 은 저장된 판정과 **완전히 일치한다**(이동 0 건) — 재계산 경로 자체가 옳다는 확인이다.

**분류 9 는 123 건에서 0 건으로 붕괴한다.** 보고서가 게재한 분류 9 는 전부 절단 조인의
산물이었다. 이 역시 방향이 **우리에게 불리하다.**

**그러나 분류 7·8·9·10 을 다시 확정할 수는 없다.** 분류는 `(r_v, r_m)` 두 축으로 갈리는데
`r_v`(VWorld 좌표 기준 심판)를 다시 계산하려면 VWorld 순방향 좌표가 필요하고, 그 좌표는
보존되지 않았으며 순방향 재호출은 금지다. 저장된 `r_v` 역시 **같은 절단 조인으로 계산됐으므로
그대로 쓸 수 없다.** 따라서 수선 후 상태에서 C 군은 구간으로만 말할 수 있다 —
`r_m = 거짓` 인 155 건은 `{8, 10}`, 자료 부재 61 건은 분류 11 이다. **`r_v` 는 미확인이다.**

> **[정정 2026-08-28 · T050]** **절단 조인 결함의 실증·수선·분류 9 붕괴(123→0)는 전부
> 유효하다** — 측정계 코드의 결함이며 런타임 오염과 무관하다. 철회하는 것은 마지막 해석
> 한 줄이다: "우리 순방향은 그 123 건에서 리를 틀린다" — 그 top-1 은 리 좁힘이 꺼진
> 런타임의 출력이므로 §5.5 와 같은 아티팩트다(§0.0). 현행 재질의에서 실패군 b_code 일치
> 99.92%.

---

## 8. 표본 B — 부존재 지번에 대한 응답 행태

**이 절의 모든 비율은 "교란 질의 집합에서의 발생률"이다.** 실사용 트래픽의 발생률이 아니다.
실재하지 않는 지번을 인위적으로 만들어 넣은 집합이므로, 여기 수치를 "우리 지오코더가 N% 확률로
헛것을 반환한다"로 읽으면 틀린다.

표본 B 1,200 건(지번 30 층 × 40 건), 표본 A 와 중복 0 건. 역방향은 돌리지 않는다.
소요 694.6 초.

| 항목 | 값 |
|---|---|
| VWorld `NOT_FOUND` | **1,062 (88.50%)** — 부존재를 부존재로 판정 |
| VWorld `OK` | 138 (11.50%) — VWorld 도 무언가를 반환 |
| 게이트 E1 / E2 | 1,062 / 3 · D0 = 138 |
| 우리 8092 가 주소 후보를 반환 | **160 / 1,200 = 13.33%** |
| **VWorld 가 부존재로 확정한 건 중 우리가 답을 낸 비율** | **43 / 1,062 = 4.05%** |
| 우리 오라클 `O=N` | 1,200 (100%) — 우리 DB 에도 없음이 확인됨 |
| 본번 근사 `o_apx` 참 | 1,099 (91.58%) — 본번은 있고 부번만 없다 |
| D0 138 건의 분류 | 5:99 · 6:12 · 3:20 · 4:1 · 11:6 |

**성능 관측.** 부존재 질의에서 우리 순방향 지연이 **평균 1.841 s · p95 4.800 s** 로,
표본 A(평균 0.027 s · p95 0.114 s)의 **약 68 배**다. 재시도 59 회 중 3 건은 끝내 실패해
`HTTP_503` 을 반환했다(E2 3 건). 부존재 질의가 전면 탐색을 유발하는 것으로 보이며,
이는 가용성 관점의 별도 위험이다.

---

## 9. 상용 대비 우리 위치 — 판정

**도로명 주소에서는 상용 기준과 실질적으로 대등하고, 농촌 지번 주소에서는 명확히 뒤진다.**
도로명층은 응답률 99.92%, 100 m 임계 통과 96.14%(가중 97.55%), 1 km 초과 완전 실패 0.15%,
top-1 이 상위 5 중 최근접인 비율 99.88% 로 순위 결함이 사실상 없다. 지번층은 좌표 자체가
VWorld 와 밀리미터 단위로 일치하지만(p50 4 mm) — 이는 §0.1 대로 같은 지적 원천을 공유하는
데서 오는 것이라 정확도 근거가 아니다 — **농촌 지번의 47.95% 가 1 km 넘게 빗나가고,
그 84.18% 는 정답을 상위 5 안에 갖고도 순위에서 밀린다.** 도시 지번은 3,163 건 중 1 km 초과가
0 건이므로 이 격차는 규모나 좌표 품질의 문제가 아니라 **리(里) 식별 실패라는 단일 결함**이다.
같은 뿌리가 역방향에서도 나타나 우리는 법정동코드에 리를 담으면서 주소 문자열에서는 리를
**사실상 전량 누락**한다 — C 단계 60 층 재측정 실측 채움률 지번 **0.00%**(0/5,064) ·
도로명 **0.27%**(16/5,979), 같은 지점에서 VWorld 는 42.69% · 44.94% 를 채운다(§6.7).
(A·B 단계 판은 이 자리에 "100% 누락"이라고 적었다. 정확히 0 은 아니므로 실측치로 바꾼다.)
그리고 이 결함은 렌더링에만 머물지 않는다 — **우리 심판 축이 이웃 리의 동일 번지를 정답으로
세고 있었고**(§7.7), 뒤집힌 123 건이 전부 "다른 리의 같은 번지"였다. 리 식별 실패가
순방향·역방향·내부 판정 세 곳에서 같은 모양으로 나타난다. 여기에 전남광주 통합 개편 미반영(§5.7)이 겹쳐
법정동코드 일치율이 지번 70.63%·도로명 90.75%에 머문다. **요약하면, 우리 지오코더의 약점은
좌표 정밀도나 커버리지가 아니라 행정구역 식별 — 특히 리 단위 — 에 있다.** 이 축은
자기참조가 가려주지 못하는 축이며, 그래서 이 판정은 좌표 원천을 공유한다는 한계에도 불구하고
유효하다.

> **[정정 2026-08-28 · T050]** 이 판정문 중 **도로명 대등 판정은 유지**된다(리 사전과 무관한
> 축, §0.0.2). 다음은 **철회**한다: 농촌 지번 47.95% 열세 · "리 식별 실패라는 단일 결함" ·
> 리 문자열 전량 누락 — 전부 사전 미기동 아티팩트이며 현행 재질의에서 실패군 99.92% 회복,
> 대조군 무회귀(§0.0). 전남광주 미반영은 202605 세대 사실이었으나 T045 재빌드로 빌드측 해소.
> **따라서 "약점은 행정구역 식별— 특히 리 단위"라는 요약은 더 이상 성립하지 않는다.**
> 신세대(202607+T049) 기준의 위치는 §0.0.3 재측정이 확정한다.

---

## 10. 개선 우선순위 (§4.6)

**점수 = (가중 발생빈도) × (회수 가능 비율 r) ÷ (구현 난이도 c)**
`r`·`c` 는 계획 §4.6 이 고정한 값을 그대로 쓴다. 가중 CI 반폭은 지번 ±1.638 %p · 도로명 ±1.715 %p.

### 10.1 T047 범위 안

| 순위 | 분류 | 가중 발생빈도 | `r` | `c` | 점수 | 근거 |
|---:|---|---:|---:|---|---:|---|
| **1** | **6 후보 순위 오류 (지번)** | **19.95% ± 1.64** | ≈1.0 | 하 | **높음** | 정답이 상위 5 안에 있다(84.18%). 랭킹에 리 가중치를 넣으면 회수. §5.5 |
| 2 | 1 파서·조회 실패 | 0.05% (지번) · 0.08% (도로명) | ≈1.0 | 하 | 낮음 | 건수 자체가 작다 |
| — | 3 근사매칭 부재 | **0.00%** | 0.5~1.0 | 중 | **0** | 이 표본에서 관측되지 않았다. 도달가능성은 증명돼 있다 |

**1 순위의 기대 효과.** 지번층 1 km 초과 오답 1,334 건 중 1,123 건(84.18%)이 상위 5 안에
25 m 이내 정답을 갖고 있다. 순위만 고치면 지번 ≤25 m 통과율이 **76.40% → 최대 95.29%**
(상위5 최선 96.35%가 상한)로 오른다. 전 표본 기준 약 **+18.9 %p** 다.
단일 항목으로 이 측정에서 가장 큰 개선폭이며, 난이도도 낮다.

> **[정정 2026-08-28 · T050]** 1 순위(리 가중치 랭킹)는 **표적 부존재**로 판명 — 기대 효과
> +18.9 %p 는 이미 켜진 리 좁힘이 실현하고 있었다(현행 실패군 99.92%, §0.0). T047 은 표적을
> **P0 부팅 침묵 실패 방어·탐지·차단 + P1 부존재 질의 처리**로 재정의해 완료했다(P1 은
> §8 표본 B 결함의 수선 — S1 비주소 top-1 93→0%, S2 회귀 0 유지). §10.2 의 "리 문자열 렌더링
> 누락" 항목도 같은 이유로 철회, "전남광주 통합 코드 미반영"은 T045(2026-08-28)가 완료.

### 10.2 범위 밖 — 재빌드·좌표 파이프라인·응답 렌더링

| 분류/항목 | 가중 발생빈도 | `r` | `c` | 수선 주체 |
|---|---:|---:|---|---|
| 2 적재 커버리지 결손 | 0.00% (미관측) | ≈1.0 | **상** — 재빌드 | T045 |
| 8 우리 좌표 이상 | 0.00% (유효 분모 0) | ≈1.0 | **상** — 좌표 파이프라인 | T043 / T045 |
| **리 문자열 렌더링 누락** | 농촌 역방향 100% | ≈1.0 | 중 | `geocode-api-pg.py` (§6.4) |
| **전남광주 통합 코드 미반영** | 해당 시도 전건 | ≈1.0 | **상** — 빌드 | 빌드 파이프라인 (§5.7) |
| **구 시도코드 42·45 오라클 오판** | 표본의 13.00% | 0.991 | 하 | **B 단계에서 수선 완료** (§7.6) |

### 10.3 우선순위 대상이 아닌 것

분류 4(양측 부재) · 7(VWorld 측 의심) · 9(좌표 정의 차이)는 `r = 0` 이다 — 우리 결함이 아니다.
분류 10(양측 이탈, 지번 0.35%)은 추가 조사 없이 회수율을 추정하지 않는다.
분류 11(심판 부재)은 판정 불가 건이므로 우선순위 대상이 아니다.

---

## 11. 재현 절차

```bash
WT=<워크트리>
# 1) 표본 — 이미 만들어진 것을 쓴다. 재생성하면 다른 표본이 나온다(§3.5)
#    ~/geocode-build/t046/sample/{sample_a.jsonl,sample_b.jsonl,strata.json,manifest.json}

# 2) 본 측정
/usr/bin/python3 "$WT/scripts/t046/measure.py" --sample A --tag main
#    ★ 중단 후 이어서 돌릴 때는 반드시 --resume 를 붙인다.
#      붙이지 않으면 기존 판정파일을 os.remove 로 지우고 처음부터 다시 돈다

# 3) 표본 B (역방향은 자동으로 꺼진다)
/usr/bin/python3 "$WT/scripts/t046/measure.py" --sample B --tag perturb

# 4) 집계 — --strata 를 반드시 명시한다(기본값은 크래시한다, §12.5-5)
/usr/bin/python3 "$WT/scripts/t046/aggregate.py" \
    --verdicts ~/geocode-build/t046/out/verdict_main.jsonl \
    --strata   ~/geocode-build/t046/out/population.json \
    --layer jibun            # 및 --layer road, 각각 --relaxed 병행

# 5) 역방향 재측정 (C 단계, §6.7). 순방향 VWorld 를 부르지 않으므로 예산 소모 0.
#    두 기준을 **순차로** 돌린다 — 동시 실행하면 VWorld 요청률이 16 req/s 가 된다.
/usr/bin/python3 "$WT/scripts/t046/measure.py" --sample A --reverse-only --basis src --tag src
/usr/bin/python3 "$WT/scripts/t046/measure.py" --sample A --reverse-only --basis fwd --tag fwd
/usr/bin/python3 "$WT/scripts/t046/aggregate_rev2.py" --tag src   # 및 --tag fwd

# 6) C 단계 결함 실증 (§7.6.1 · §7.7). 둘 다 PostGIS 조회뿐 — VWorld 를 부르지 않는다.
/usr/bin/python3 "$WT/scripts/t046/verify_pip_ri.py"   # 뒤집힌 123 건의 PIP 실증
/usr/bin/python3 "$WT/scripts/t046/reclass_axes.py"    # S0/S1/S2 네 칸 재분류 대조
```

`population.json` 은 `strata.json` 의 `n_h` 를 `{층 이름: N_h}` 로 펴낸 것이다(60 층, ΣN_h 17,141,159).
재현에 필요한 세 가지(시드·제외 규칙·원천 해시)는 §3.5 에 적었다.

`verify_pip_ri.py`·`reclass_axes.py` 의 `S0`(= 보고서 게재값 상태)는 **재구성이 아니라
`git show 8f87b9c:scripts/t046/oracle.py` 로 뽑은 커밋 파일 자체**를 불러 돌린다. 지금 코드로
옛 동작을 흉내내면 그 흉내가 맞는지를 다시 증명해야 하기 때문이다. 참조는 `T046_HEAD_REF`
환경변수로 바꿀 수 있다(기본값은 D 단계에서 리베이스 후 SHA 로 갱신했다, §12.1). **두 스크립트 모두 `stored->S0` 이동이 0 건**이다 — 재계산 경로가
저장된 판정을 정확히 재현한다는 뜻이고, 그래야 전후 차이를 수선의 효과로 읽을 수 있다.

---

## 12. 부록

### 12.1 측정 스크립트 커밋

`41298f7` — `feat(t046): VWorld 외부기준 측정 파이프라인 구현 — 층화표본·존재오라클·분류·집계 (T046)`.
`scripts/t046/{sample,geodist,utmk,normalize,oracle,classify,measure,aggregate,pgprobe}.py`
및 `tests/t046/test_*.py` 8 개(278 테스트).
B 단계의 수선(역방향 스키마 v2, 오라클 구 시도코드 보정)은 `8f87b9c` 에 담겼다.
수선 후에도 278 테스트 전건 통과를 확인했다.

**C 단계 추가분**(이 보고서와 같은 커밋):

| 파일 | 내용 |
|---|---|
| `scripts/t046/revmetrics.py` | §4.2 축 추출·비교·집계 **순수 함수**. HTTP 없이 전부 시험 가능하게 분리 |
| `scripts/t046/aggregate_rev2.py` | 역방향 재측정 집계 → 마크다운 |
| `scripts/t046/reclass_axes.py` | S0/S1/S2 네 칸 재분류 대조(§7.6.1 · §7.7) |
| `scripts/t046/verify_pip_ri.py` | 뒤집힌 123 건의 PIP 실증(§7.7) |
| `scripts/t046/measure.py` | `--reverse-only --basis`, `type=both`, `gate_e1` 방향 한정 |
| `scripts/t046/oracle.py` | 심판·`O_apx`·대표점 축의 구 시도코드 보정, 정확 PNU 조인 |
| `tests/t046/test_revmetrics.py` · `test_measure_rev2.py` | 신규 79 개 |

테스트는 `8f87b9c` 기준 **278 → 385 개(+107)**, 전건 통과. 내역은 F3 조건 1-b 회귀 +10 ·
F-ri 리 절단 회귀 +10 · C 단계 +87(대표점 축 8 · `revmetrics` 55 · rev2 경로 24).

**D 단계 추가분**(`docs/geocode-vworld-외부검증.md` 이 절과 §6.7.3 완화 전 표, §13 대조표):

| 파일 | 내용 |
|---|---|
| `scripts/t046/strict_table.py` | 계층 4 축의 완화 전(`_strict`) 재집계 → 마크다운·JSON. **재측정 아님** — `rev2_*.jsonl` 만 읽는다 |
| `tests/t046/test_strict_table.py` | 신규 14 개(선택 규약·판정불가 제외·뒤집힘 계수·자기검증 노출) |

테스트는 **385 → 399 개(+14)**, 전건 통과.

**커밋 SHA 대응 — 리베이스 전후.** T046 은 Manager 재기동 시 `resume_no_session_id` 로
자동 abort 됐고(작업 실패가 아니다), 복구 과정에서 세 커밋을 T043(`ba903b8`) 위로
**리베이스**했다. 그래서 A~C 단계 SHA 가 전부 바뀌었다. 옛 SHA 는 어느 브랜치에도
매달려 있지 않으므로(`git branch --contains` 공집합) **gc 되면 조회할 수 없다.**

| 단계 | 리베이스 전 | 현행 | 대조 |
|---|---|---|---|
| A | `e8e2615` | `41298f7` | — |
| B | `9e26fdd` | `8f87b9c` | `scripts/t046`·`tests/t046` 18 파일 md5 **전건 동일** |
| C | `6a562ab` | `4e53719` | `scripts/t046`·`tests/t046` 전 파일 md5 **전건 동일**. `docs` 는 `data-patch-runbook.md` **1 개가 다르다**(54+/3−) — 아래 주 |

`docs` 의 그 1 개 차이는 **T046 과 무관하다.** `data-patch-runbook.md` 는 T046 커밋이
건드린 적이 없고(`git log 41298f7..4e53719 -- <그 파일>` 공백), T043(`ba903b8`) 이 바꾼
것이다. 리베이스로 base 가 T043 위로 옮겨가면서 생긴 **base 델타**이며, 같은 base 를
기준으로 보면 차이가 없다 — `git diff ba903b8 4e53719 -- <그 파일>` 이 **공백**이다.

`verify_pip_ri.py`·`reclass_axes.py` 의 `T046_HEAD_REF` 기본값은 D 단계에서 `9e26fdd` →
`8f87b9c` 로 갱신했다. **죽은 SHA 를 기본값으로 두면 재현이 깨진다**(완료조건 6). 두 판이
같은 파일임은 위 md5 대조로 확증했고, 새 REF 로 로드한 판이 **무보정 절단 판**임은
**SQL 결합 유무**로 판별했다 — 로드된 판의 `sql_referee_parcel_batch()` 에 정확 PNU 결합
`p.pnu = k.pnu` 가 **없고**(현행에는 **있다**), 절단 판이 아니면 `AssertionError` 로 죽는
가드가 그대로 작동한다. 서명은 옛 REF 판 `referee_parcel_batch(self, keys)` · 현행
`(self, keys, legacy=True)` 로, 옛 판에는 legacy 인자 자체가 없다. **판별 근거는 서명이
아니라 SQL 결합 유무다** — 서명은 인자 이름이 바뀌어도 성립할 수 있지만 결합 조건은
동작을 직접 규정한다.

### 12.2 커밋 메시지의 수치 오류

커밋 `41298f7`(구 `e8e2615`) 메시지는 PNU 개별 컬럼 바인딩의 이득을 **263 배**로 적었다
(`oracle.py` 모듈 docstring 에도 같은 수치가 있다). B 단계 재현값은 **382 배**,
`EXPLAIN` 기준으로는 **508 배**다. 커밋 메시지는 정정할 수 없으므로 여기에 남긴다.
인용할 때는 382 배(또는 EXPLAIN 508 배)를 쓰라.

### 12.3 시간과 예산

| 구간 | 시각 | 건수 | 소요 |
|---|---|---:|---:|
| 표본 A v1 | 14:15:03 → 14:58:44 | 8,000 | 2,621 s |
| (스키마 교체) | 14:58:44 → 15:03:07 | — | 263 s |
| 표본 A v2 | 15:03:07 → 15:21:40 | 4,000 | 1,104 s |
| **표본 A 합** | 14:15:03 → 15:21:40 | **12,000** | **3,997 s (66.6 분)** |
| 표본 B | 15:21:51 → 15:33:29 | 1,200 | 695 s (11.6 분) |
| 역방향 재측정 `src` (C) | 18:05:57 → 18:56:59 | 12,000 | 3,061 s (51.0 분) |
| 역방향 재측정 `fwd` (C) | 18:57:06 → 20:02:27 | 12,000 | 3,915 s (65.3 분) |

**C 단계의 순방향 VWorld 호출은 0 건이다.** 역방향 23,054 회를 추가로 썼으나 역방향에는
일일 한도가 없으므로(§1.1) **예산 잔량은 A·B 단계 종료 시점과 같다.** 두 창을 순차로 돌린
것은 동시 실행 시 VWorld 요청률이 16 req/s 가 되어 8 req/s 규약을 깨기 때문이다.

`fwd` 창이 `src` 보다 28% 느린 것은 우리 8092 순방향 12,003 회가 더해졌기 때문이 아니다
(순방향 p95 는 0.110 s 에 불과하다). 우리 역방향 p95 가 1.645 s → 2.153 s 로 올라간 것,
즉 **우리 서버가 약 2 배 부하에서 느려진 것**이 주된 몫이다. 같은 부하에서 `/reverse`
503 이 12 건 발생했다(§6.7.8).

계획 예산은 **55.6 분**이었다. 측정만 따지면 3,725 s(62.1 분, **+11.7%**), 스키마 교체
공백을 포함하면 66.6 분(**+19.8%**)이다. 처리율은 3.22 건/s 로 계획(약 3.60 건/s)보다 낮다.

**예산 초과의 원인 지목은 수정되어야 한다.** 계획·인계문은 원인을 우리 8092 역방향 지연
(`ours|rev` p95 3,031.7 ms)으로 지목했으나, 본 측정에서 실측된 `ours|rev` p95 는
**785.6 ms** 로 그 1/4 이다. 예열 구간의 콜드 캐시 수치를 본 측정에 투영한 것으로 보인다.
본 측정의 지연 병목은 특정 축이 아니라 전반적 처리율이다.

### 12.4 진단 파일

`--diag` 를 쓰지 않았으므로 **진단 파일 생성 0 건**이다(`run_main.json` 의
`diag_written: 0` 으로 확인). `~/geocode-build/t046/diag/` 에는 실행 로그와 A 단계
벤치마크 기록만 있고 `diag_*.jsonl` 은 존재하지 않는다.
**생성 0 건이므로 삭제 대상이 없고, 삭제 시각·확인 방법도 해당 없음이다.**

### 12.5 A 단계에서 발견된 계측·도구 결함

B 단계가 실행 중 발견한 것들이다. 전부 재현 확인했다.

| # | 결함 | 영향 | 상태 |
|---|---|---|---|
| 1 | `_reverse_bcode` 가 없는 키 `results` 를 읽음 | 역방향 주소축 전건 `None` | **v2 에서 수선.** v1 구간(8,000 건) 복구 불가 |
| 2 | `address_source` 미기록 | M8 분리 불가 | **v2 에서 수선** |
| 3 | 오라클이 구 시도코드 42·45 를 보정하지 않음 | 1,560 건(13.00%) 을 `O=N` 오판 | **수선.** 분류표 영향은 0 (§7.6) |
| 4 | 역방향 `type=PARCEL` 고정 | §4.2-a 예비조사 불가 | 미수선(§6.5) |
| 5 | `aggregate.py --strata` 기본값이 `sample/strata.json` | 그 파일은 `{"n_h":…}` 형태라 `load_population` 이 빈 dict 를 만들고 `effective_n` 이 `None` → `TypeError`. **기본값으로 실행하면 반드시 크래시한다** | 우회: `population.json` 을 만들어 `--strata` 명시 |
| 6 | `aggregate.py` 의 F6 행이 구조적으로 항상 0 | 완화 필요성이 과소보고됨 | 문서화(§7.3) |
| 7 | `--resume` 없이 재실행하면 판정파일을 `os.remove` | 8,000 건 소실 위험 | 문서화(§11) |
| 8 | 재호출 일치율 모드 부재 | §8 항목 16 불가 | 미수선 |
| 9 | `run_*.json` 이 재개마다 덮어써짐 | 앞 구간 재시도·지연 계수기 소실 | 문서화(§4.4) |
| 10 | `Counters.gate_e1` 이 호출 **방향을 가리지 않음** | 계수기 19 vs 실제 E1 18. 차이 1 은 **역방향** `NOT_FOUND` 다(실측 `vworld\|fwd\|NOT_FOUND` 18 · `vworld\|rev\|NOT_FOUND` 1). E1 은 순방향 게이트이므로 역방향 실패를 더하면 안 된다 | **C 단계에서 수선**(`d == "fwd"` 로 좁힘 + 회귀 테스트 2 개). **보고 수치에는 영향 없다** — 보고서의 87 은 판정 레코드에서 도출했다 |
| 11 | 구 시도코드 보정이 **심판·`O_apx` 축에 없었음** | 분류 11 의 22 건(30.6%)이 이 결함 기인. 그럼에도 §6.6·§12.5 는 "완료"로 표기 | **C 단계에서 수선.** 정정 경위는 §7.6.1 |
| 12 | 오라클 조회 키가 **리 2 자리를 절단** (`emd_cd = pnu[:8]`) | 심판이 "같은 읍면동 안 아무 동일번지 필지"를 물었다. 분류 9 의 123 건 전부가 이 산물 | **C 단계에서 수선**(정확 PNU 19 자리 조인). 실증은 §7.7 |

### 12.6 원천 파일 32 개

행안부 202607 내비게이션용DB, 경로 접두 `~/geocode-build/staged/navi/` 는 생략했다.

| # | 파일 | 바이트 | SHA-256 |
| ---: | --- | ---: | --- |
| 1 | `match_build_busan.txt` | 98,470,440 | `ac7631684b98570c2319f56362d920da4170343a30bd8d9370807def6d057f78` |
| 2 | `match_build_chungbuk.txt` | 165,440,581 | `e3c6e5c122631b42b0aa1ed26c371cb854fcda2728e167fda7ceaabf845a7697` |
| 3 | `match_build_chungnam.txt` | 231,081,965 | `64af8e62aae10a08fdeb8d46f335ee85bb92cbd464c69d9ecabc4257f46ddff2` |
| 4 | `match_build_daegu.txt` | 72,593,419 | `78ff7958e4d8890e8fabab25226384e8fd6103573c6c4be89f851c7f991d1648` |
| 5 | `match_build_daejeon.txt` | 35,008,751 | `a065899da3c265f12773630aae95f2bb4c98b579d4c6b4691905b13694b81f9c` |
| 6 | `match_build_gangwon.txt` | 154,430,503 | `565181143f0c582d88ce4d9c0f900b347bbdfa70243473cda29746ced6afe8de` |
| 7 | `match_build_gyeongbuk.txt` | 365,727,328 | `9d65af5add64712462971a9c1cf5644d285c3ae5971d18a8ba47b1d13fad1f3f` |
| 8 | `match_build_gyeongnam.txt` | 332,089,245 | `4c03eab1a666172918e81f470874674885c40226fbd48cd5c3f2502bddadc296` |
| 9 | `match_build_gyunggi.txt` | 432,087,352 | `4fe10c6c271e763c8dd25e417ed1e865fed085b150e63e2f55873c9f8414c0a7` |
| 10 | `match_build_incheon.txt` | 65,042,684 | `4d15c47c9832f1b0ff96d7d8e073d74513440569716447406874eaf749abf585` |
| 11 | `match_build_jeju.txt` | 66,417,206 | `72dc35216283d73453c1ce3095e4b8ef87ffd4ccd7f5bffffaaa444e326d1891` |
| 12 | `match_build_jeonbuk.txt` | 204,117,516 | `a8b3cc02afcce18ee292085fa8e4270b544c1a7940e205481c3c581558c66f02` |
| 13 | `match_build_jeonnamgwangju.txt` | 347,962,340 | `d4ccb4c566ed6854a2c8ac0201b1ed5728660891486bb8ec24041b9800bc01b0` |
| 14 | `match_build_sejong.txt` | 13,436,174 | `32b396ba48508824a8adf6a06e53956dcb617ff42522558f71540e9e7226d9cc` |
| 15 | `match_build_seoul.txt` | 150,812,846 | `623a98ce2f1412fe92188edb99bfa3cd8afc80778d1520cf3a05fa1864abc886` |
| 16 | `match_build_ulsan.txt` | 39,852,399 | `cfdab294f4fcf03f45b35f29b8d86fec37ec4d87c5d3345478fca4d4d6013dc4` |
| 17 | `match_jibun_busan.txt` | 47,168,326 | `116bfc356ffa052a8bb63aee695cbcd497b6920c5eb037c0799009e19ea4af79` |
| 18 | `match_jibun_chungbuk.txt` | 71,311,781 | `9569b322c890a51e8aa83a20545aee66aaa8d5c4efac12d10afa4ab892d6f3ee` |
| 19 | `match_jibun_chungnam.txt` | 112,122,342 | `53a3ae85f7c463a3879c8efd0be0abee22f10acca9c3e93b2c90ea545019d48a` |
| 20 | `match_jibun_daegu.txt` | 35,322,302 | `aa2c6e7895f8669b01211facc5b9f8955068d62fbcaa253979f9024ea36a8acf` |
| 21 | `match_jibun_daejeon.txt` | 16,793,575 | `6e52164b29f8cf8fc9b98a63bfb54516370882125c76e24e82d88a4926eb001f` |
| 22 | `match_jibun_gangwon.txt` | 88,355,292 | `eb05e1c0f08f3058fb0374300083eca2acdb5cbe71f27440b76910215fe658d8` |
| 23 | `match_jibun_gyeongbuk.txt` | 139,888,493 | `6ca88faa51b326686a45729bb8ac41f64b40a168c665c9bac3055cc2e6749f80` |
| 24 | `match_jibun_gyeongnam.txt` | 154,439,476 | `6ef8610b5d7d9456714f2c38e6c2f04f5b1a063a77e729413f8ed1a204a69553` |
| 25 | `match_jibun_gyunggi.txt` | 203,996,955 | `1dcadb66e650c439837c5a2d9ced8dc7d4651e64270b49e87d9f2be907431ee2` |
| 26 | `match_jibun_incheon.txt` | 31,075,720 | `f4caaa5a1e8cd2a18c8eb3308f28f1423e2e03cf953bf650f9953fc29d5573d0` |
| 27 | `match_jibun_jeju.txt` | 27,224,320 | `b2569413b32b44c9b213a2192b1ca5863bf3a1e68178106ec21bada7da897cbe` |
| 28 | `match_jibun_jeonbuk.txt` | 89,695,620 | `e6560c3f03d4c45eb5f79598aa6dca32d7b31b3cfde59e9769d9d133669eb79b` |
| 29 | `match_jibun_jeonnamgwangju.txt` | 162,091,606 | `cf3532f6055ef81e6a9c57166e9ae28c30a24ee2cd60fe81e56af3886f4fd8fb` |
| 30 | `match_jibun_sejong.txt` | 4,788,944 | `12539aa81d93da311010488baa620d920e28f477c1b8d5a7700e422c0865221b` |
| 31 | `match_jibun_seoul.txt` | 85,249,869 | `9aa29e760cdd4e5ceda737bc209c977f337053feb4a199572c6c4a27bc06ae73` |
| 32 | `match_jibun_ulsan.txt` | 17,155,133 | `3da79dd772421b10f619be274b3edb2cffc33b235a52ae66a26d320b4a58864e` |
| | **합계 32 개** | **4,061,250,503** | 집계 해시 `9d0fc6ac167d1f07bd4bc9d1e811defdf4b6c84151cf2bf81b8e73c7d7b28b5a` |

---

## 13. 완료조건 7 항 대조표 (태스크 §5)

행 번호는 **이 문서 기준**이다. 미충족·부분 충족은 "충족"으로 뭉개지 않는다.

| # | 완료조건 (태스크 §5 문언) | 충족 절 | 행 | 판정 |
|---|---|---|---|---|
| 1 | 표본 설계와 크기의 근거, 시드, 재현 절차 | §3.1 층 정의·크기 / §3.2 시드·파생식 / §3.3·§12.6 원천 32 개 해시 / §3.4 균등배분 비용 / §3.5 재현 한계 / §11 재현 절차 | 287–311 · 312–321 · 322–328 · 1452–1493 · 329–346 · 347–360 · 1287–1329 | **충족** |
| 2 | §4.1 · §4.2 지표 전체 (측정 대상 판을 명시) | 대상 판 §1.1·§1.2 / 순방향 §5.1–5.8 / 역방향 §6.1–6.4 · §6.7(60 층) / 자기판정 §6.8 | 185–200 · 201–224 · 440–581 · 587–725 · 726–956 · 957–992 | **지표·대상 판 충족 / 좌표 규약 미충족** (아래 주 1) |
| 3 | §4.3 실패 원인 분류표 | §7.1 배타 분류표 / §7.2 두 분모 / §7.3 직교 플래그 / §7.5 심판 부재 / §7.6·§7.6.1·§7.7 오라클·심판축 결함 | 1001–1018 · 1019–1030 · 1031–1048 · 1059–1082 · 1083–1200 | **충족** (아래 주 2) |
| 4 | **"상용 대비 우리 위치"에 대한 한 문단 판정** — 숫자로 뒷받침 | §9 | 1228–1250 | **충족** (아래 주 3) |
| 5 | **개선 우선순위** — 어느 원인을 먼저 잡아야 지표가 가장 크게 오르는지 | §10 머리 / §10.1 T047 범위 안 / §10.2 범위 밖 / §10.3 대상 아님 / 수선 명세 §6.6 | 1251–1255 · 1256–1268 · 1269–1278 · 1279–1286 · 709–725 | **충족** |
| 6 | 측정 스크립트를 커밋 (재현 가능해야 한다) | §12.1 커밋 4 개·파일 표·신구 SHA 대응 / §11 재현 절차 | 1332–1389 · 1287–1329 | **충족** (아래 주 4) |
| 7 | VWorld 호출 실패·한도 초과 건수와 그 처리 방식 | §4.1 총계 / §4.2 `status` 분포 / §4.3 게이트 / §4.4 재시도·계수기 소실 / §4.5 관측된 이상 / §6.7.8 재측정 창 | 363–373 · 374–391 · 392–401 · 402–423 · 424–435 · 919–956 | **충족** (아래 주 5) |

### 주 1 — 완료조건 2 를 "충족"으로 단정하지 않는 이유

두 갈래로 갈린다. **지표 산출**은 충족이다 — 9 개 지표군이 **60/60 층** 산출됐고(§6.7),
2 차 검수자가 원자료에서 축 40 칸·리 16 칸·`address_source`·교집합·호출 집계를 독립
재계산해 **전건 일치**를 확인했다. **측정 대상 판 명시**도 충족이다(§1.1·§1.2·§0.3.1).

**미충족은 계획 §4.2 의 질의 좌표 규약이다.** 역방향은 "VWorld 순방향이 돌려준 좌표"로
물어야 했는데, 그 좌표가 판정 레코드에 보존되지 않았고(§3.3 이 좌표를 버린다) 순방향
재호출은 금지라 **원리적으로 복구 불가**다. C 단계는 표본에서 재유도한 두 대체 기준
(`src`·`fwd`)으로 측정했고, 두 기준의 차이가 ≤0.14 %p 임을 §6.7.7 에서 계량했으나
**원본 기준과의 차이는 잴 수 없다.**

판정이 갈린 사실도 그대로 적는다. 구현자는 좌표 규약 위반을 이유로 **충족 선언을
거부**했고(§6.8), 2 차 검수자는 "§5-2 는 좌표 규약을 완료조건으로 걸지 않았다 —
완료조건은 산출물(지표 전체 + 대상 판 명시)을 요구한다"며 **충족**으로 판정했다.
**D 단계는 어느 쪽으로도 뒤집지 않는다.** 이 항을 무조건 닫으려면 순방향 재호출이
필요하고 그것은 금지돼 있다. 좌표 보존은 **T047 이월**이다(§6.6).

### 주 2 — 분류표의 잔여 한계

배타 11 분류는 전건 산출됐다. 다만 C 단계의 리(里) 절단 수선(§7.7)으로 분류 9 가
정의상 0 으로 붕괴하며, 뒤집힌 123 건이 분류 8 과 10 중 어디로 가는지는 `r_v`(VWorld
순방향 반환 좌표 기준 심판)를 요구한다. 그 좌표가 없으므로 **구간으로만 서술**했다.
모르는 것을 안다고 쓰지 않았다는 뜻이며, 확정에는 순방향 재호출이 필요하다.

### 주 3 — "한 문단 판정"의 숫자 근거

§9 는 한 문단이고 다음 수치로 뒷받침된다: 도로명층 응답률 99.92% · 100 m 통과
96.14%(가중 97.55%) · 1 km 초과 0.15% · top-1 최근접 99.88%; 농촌 지번 1 km 초과
47.95%, 그중 84.18% 가 상위 5 안에 정답 보유; 도시 지번 1 km 초과 0/3,163;
리 채움률 지번 0.00%(0/5,064) · 도로명 0.27%(16/5,979) 대 VWorld 42.69% · 44.94%;
법정동코드 일치 지번 70.63% · 도로명 90.75%. 판정 문장과 §0.1 의 자기참조 한계가
충돌하지 않는 이유도 같은 문단에 적혀 있다.

### 주 4 — 재현성은 D 단계에서 한 번 깨졌다가 복구됐다

리베이스로 A~C 단계 SHA 가 전부 바뀌었는데 `verify_pip_ri.py`·`reclass_axes.py` 가
**리베이스 전 SHA 를 기본값으로 물고 있었다.** 그 커밋은 어느 브랜치에도 매달려 있지
않아 gc 되면 두 스크립트가 깨진다 — 완료조건 6 의 "재현 가능해야 한다"에 정면으로
걸린다. D 단계에서 기본값을 현행 SHA 로 갱신하고, 두 판이 같은 파일임을 md5 로
확증한 뒤 신구 대응표를 §12.1 에 남겼다.

### 주 5 — 호출 실패·한도의 요지

`OVER_REQUEST_LIMIT` 는 **0 건**이다(한도 초과 없음). 실패는 `status` 별로 분해돼
있고 **부재(`NOT_FOUND` 류)와 전송실패(HTTP 오류)를 구분**한다 — 부재를 성공으로도,
실패로도 뭉개지 않는다. 재시도 정책과 포기 건수는 §4.4 에 있으며, 같은 절이 **본 측정
앞 구간의 계수기가 재개 시 덮어써져 소실된 사실**을 숨기지 않고 적는다. 재측정 창의
호출 집계는 §6.7.8 에 따로 있고, 그 창에서 VWorld **순방향 호출은 0 건**이다
(`counters.calls` 에 `vworld|fwd` 키 자체가 없다).
